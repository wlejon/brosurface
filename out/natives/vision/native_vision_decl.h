// native_vision_decl.h — the C entry points behind idl/vision.idl, one prototype per
// native. Generated by brosurface (gen/emit_natives.mjs); do not edit. The bodies
// are hand-written in bro against the engine, and the compiler checks them here.
//
// How each type crosses (schema/native_types.mjs, bronze/src/abi/bronze_native_type.h):
//   double / int32_t / bool      f64 / i32 / bool
//   const char*                  str — UTF-8, valid for the call only; a RESULT must
//                                outlive the return (a per-thread scratch), and one
//                                marked JSON carries JSON of the IDL type in the comment
//   uint64_t                     dynamic — a callback's raw Value bits; keep it in a
//                                Persistent, never across an allocating call raw
//   (const T*, uint32_t)         a typed array: elements and count, valid for the call
//   void*                        a class handle: what the class constructor returned
//   bronze_native_buffer* out    a typed-array RESULT: the function returns void and
//                                fills *out (data, length in elements); release == NULL
//                                and the runtime copies during the call; release set
//                                ([transfer]) and the array is a view over data, with
//                                release(ctx) run when it is collected
//   bool <x>_given, T <x>        an optional value with no declared default, and
//                                whether it was passed
//
// A dictionary or list RESULT crosses as reads: the operation runs and keeps its
// result in a per-thread slot, answering nothing (or the list's length), and the
// <op>_<member> / <op>_at natives read from that slot until the next call.
#pragma once

#include <stdbool.h>
#include <stdint.h>

#include "abi/bronze_native_type.h"  // bronze_native_buffer, owned by bronze

#ifdef __cplusplus
extern "C" {
#endif

// destructor of __bro_native.vision.DepthEstimator: frees what the constructor (or a native returning a DepthEstimator) allocated
void bro_vision_DepthEstimator_dtor(void* self);

// constructor of __bro_native.vision.DepthEstimator: registered so the class exists (instances come from natives returning a DepthEstimator); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.DepthEstimator
void* bro_vision_DepthEstimator_ctor(void);

// destructor of __bro_native.vision.Sam: frees what the constructor (or a native returning a Sam) allocated
void bro_vision_Sam_dtor(void* self);

// constructor of __bro_native.vision.Sam: registered so the class exists (instances come from natives returning a Sam); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.Sam
void* bro_vision_Sam_ctor(void);

// destructor of __bro_native.vision.NormalEstimator: frees what the constructor (or a native returning a NormalEstimator) allocated
void bro_vision_NormalEstimator_dtor(void* self);

// constructor of __bro_native.vision.NormalEstimator: registered so the class exists (instances come from natives returning a NormalEstimator); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.NormalEstimator
void* bro_vision_NormalEstimator_ctor(void);

// destructor of __bro_native.vision.Hed: frees what the constructor (or a native returning a Hed) allocated
void bro_vision_Hed_dtor(void* self);

// constructor of __bro_native.vision.Hed: registered so the class exists (instances come from natives returning a Hed); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.Hed
void* bro_vision_Hed_ctor(void);

// destructor of __bro_native.vision.Lineart: frees what the constructor (or a native returning a Lineart) allocated
void bro_vision_Lineart_dtor(void* self);

// constructor of __bro_native.vision.Lineart: registered so the class exists (instances come from natives returning a Lineart); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.Lineart
void* bro_vision_Lineart_ctor(void);

// destructor of __bro_native.vision.Mlsd: frees what the constructor (or a native returning a Mlsd) allocated
void bro_vision_Mlsd_dtor(void* self);

// constructor of __bro_native.vision.Mlsd: registered so the class exists (instances come from natives returning a Mlsd); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.Mlsd
void* bro_vision_Mlsd_ctor(void);

// destructor of __bro_native.vision.Openpose: frees what the constructor (or a native returning a Openpose) allocated
void bro_vision_Openpose_dtor(void* self);

// constructor of __bro_native.vision.Openpose: registered so the class exists (instances come from natives returning a Openpose); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.Openpose
void* bro_vision_Openpose_ctor(void);

// destructor of __bro_native.vision.Segformer: frees what the constructor (or a native returning a Segformer) allocated
void bro_vision_Segformer_dtor(void* self);

// constructor of __bro_native.vision.Segformer: registered so the class exists (instances come from natives returning a Segformer); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.Segformer
void* bro_vision_Segformer_ctor(void);

// destructor of __bro_native.vision.Birefnet: frees what the constructor (or a native returning a Birefnet) allocated
void bro_vision_Birefnet_dtor(void* self);

// constructor of __bro_native.vision.Birefnet: registered so the class exists (instances come from natives returning a Birefnet); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.Birefnet
void* bro_vision_Birefnet_ctor(void);

// destructor of __bro_native.vision.StyleGAN3: frees what the constructor (or a native returning a StyleGAN3) allocated
void bro_vision_StyleGAN3_dtor(void* self);

// constructor of __bro_native.vision.StyleGAN3: registered so the class exists (instances come from natives returning a StyleGAN3); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.StyleGAN3
void* bro_vision_StyleGAN3_ctor(void);

// destructor of __bro_native.vision.Dinov2: frees what the constructor (or a native returning a Dinov2) allocated
void bro_vision_Dinov2_dtor(void* self);

// constructor of __bro_native.vision.Dinov2: registered so the class exists (instances come from natives returning a Dinov2); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.Dinov2
void* bro_vision_Dinov2_ctor(void);

// destructor of __bro_native.vision.Dinov3: frees what the constructor (or a native returning a Dinov3) allocated
void bro_vision_Dinov3_dtor(void* self);

// constructor of __bro_native.vision.Dinov3: registered so the class exists (instances come from natives returning a Dinov3); never called by the wrapper, the body may return NULL
//   registered at __bro_native.vision.Dinov3
void* bro_vision_Dinov3_ctor(void);

// bro.vision.DepthEstimator.prototype.device read
//   registered at __bro_native.vision.DepthEstimator_device_get
const char* bro_vision_DepthEstimator_device_get(void* self);

// bro.vision.Sam.prototype.device read
//   registered at __bro_native.vision.Sam_device_get
const char* bro_vision_Sam_device_get(void* self);

// bro.vision.Sam.prototype.hasImage read
//   registered at __bro_native.vision.Sam_hasImage_get
bool bro_vision_Sam_hasImage_get(void* self);

// bro.vision.NormalEstimator.prototype.device read
//   registered at __bro_native.vision.NormalEstimator_device_get
const char* bro_vision_NormalEstimator_device_get(void* self);

// bro.vision.Hed.prototype.device read
//   registered at __bro_native.vision.Hed_device_get
const char* bro_vision_Hed_device_get(void* self);

// bro.vision.Lineart.prototype.device read
//   registered at __bro_native.vision.Lineart_device_get
const char* bro_vision_Lineart_device_get(void* self);

// bro.vision.Mlsd.prototype.device read
//   registered at __bro_native.vision.Mlsd_device_get
const char* bro_vision_Mlsd_device_get(void* self);

// bro.vision.Openpose.prototype.device read
//   registered at __bro_native.vision.Openpose_device_get
const char* bro_vision_Openpose_device_get(void* self);

// bro.vision.Segformer.prototype.device read
//   registered at __bro_native.vision.Segformer_device_get
const char* bro_vision_Segformer_device_get(void* self);

// bro.vision.Birefnet.prototype.device read
//   registered at __bro_native.vision.Birefnet_device_get
const char* bro_vision_Birefnet_device_get(void* self);

// bro.vision.StyleGAN3.prototype.device read
//   registered at __bro_native.vision.StyleGAN3_device_get
const char* bro_vision_StyleGAN3_device_get(void* self);

// bro.vision.StyleGAN3.prototype.zDim read
//   registered at __bro_native.vision.StyleGAN3_zDim_get
int32_t bro_vision_StyleGAN3_zDim_get(void* self);

// bro.vision.StyleGAN3.prototype.cDim read
//   registered at __bro_native.vision.StyleGAN3_cDim_get
int32_t bro_vision_StyleGAN3_cDim_get(void* self);

// bro.vision.StyleGAN3.prototype.wDim read
//   registered at __bro_native.vision.StyleGAN3_wDim_get
int32_t bro_vision_StyleGAN3_wDim_get(void* self);

// bro.vision.StyleGAN3.prototype.imgResolution read
//   registered at __bro_native.vision.StyleGAN3_imgResolution_get
int32_t bro_vision_StyleGAN3_imgResolution_get(void* self);

// bro.vision.StyleGAN3.prototype.imgChannels read
//   registered at __bro_native.vision.StyleGAN3_imgChannels_get
int32_t bro_vision_StyleGAN3_imgChannels_get(void* self);

// bro.vision.Dinov2.prototype.device read
//   registered at __bro_native.vision.Dinov2_device_get
const char* bro_vision_Dinov2_device_get(void* self);

// bro.vision.Dinov3.prototype.device read
//   registered at __bro_native.vision.Dinov3_device_get
const char* bro_vision_Dinov3_device_get(void* self);

#if BRO_WITH_VISION
// bro.vision.init
//   registered at __bro_native.vision.init
void bro_vision_init(void);
#endif

#ifdef __cplusplus
}  // extern "C"
#endif
