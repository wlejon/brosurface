// native_flora_decl.h — the C entry points behind idl/flora.idl, one prototype per
// native. Generated by brosurface (gen/emit_natives.mjs); do not edit. The bodies
// are hand-written in bro against the engine, and the compiler checks them here.
//
// How each type crosses (schema/native_types.mjs, bronze/src/abi/bronze_native_type.h):
//   double / int32_t / bool      f64 / i32 / bool
//   const char*                  str — UTF-8, valid for the call only; a RESULT must
//                                outlive the return (a per-thread scratch), and one
//                                marked JSON carries JSON of the IDL type in the comment
//   uint64_t                     dynamic — a callback's raw Value bits; keep it in a
//                                Persistent, never across an allocating call raw
//   (const T*, uint32_t)         a typed array: elements and count, valid for the call
//   void*                        a class handle: what the class constructor returned
//   bronze_native_buffer* out    a typed-array RESULT: the function returns void and
//                                fills *out (data, length in elements); release == NULL
//                                and the runtime copies during the call; release set
//                                ([transfer]) and the array is a view over data, with
//                                release(ctx) run when it is collected
//   bool <x>_given, T <x>        an optional value with no declared default, and
//                                whether it was passed
//
// A dictionary or list RESULT crosses as reads: the operation runs and keeps its
// result in a per-thread slot, answering nothing (or the list's length), and the
// <op>_<member> / <op>_at natives read from that slot until the next call.
#pragma once

#include <stdbool.h>
#include <stdint.h>

#include "abi/bronze_native_type.h"  // bronze_native_buffer, owned by bronze

#ifdef __cplusplus
extern "C" {
#endif

// destructor of __bro_native.flora.FloraWorld: frees what the constructor (or a native returning a FloraWorld) allocated
void bro_flora_FloraWorld_dtor(void* self);

// constructor of __bro_native.flora.FloraWorld: registered so the class exists (instances come from natives returning a FloraWorld); never called by the wrapper, the body may return NULL
//   registered at __bro_native.flora.FloraWorld
void* bro_flora_FloraWorld_ctor(void);

// bro.flora.FloraWorld.prototype.addVoronoiSite
//   registered at __bro_native.flora.FloraWorld_addVoronoiSite
void* bro_flora_FloraWorld_addVoronoiSite(void* self, int32_t prototypeIndex, double determinacy, double apicalControl);

// bro.flora.FloraWorld.prototype.removePlant
//   registered at __bro_native.flora.FloraWorld_removePlant
bool bro_flora_FloraWorld_removePlant(void* self, int32_t plantIdx);

// bro.flora.FloraWorld.prototype.step
//   registered at __bro_native.flora.FloraWorld_step
void* bro_flora_FloraWorld_step(void* self, double dt);

// bro.flora.FloraWorld.prototype.simTime read
//   registered at __bro_native.flora.FloraWorld_simTime_get
double bro_flora_FloraWorld_simTime_get(void* self);

// bro.flora.FloraWorld.prototype.plantCount read
//   registered at __bro_native.flora.FloraWorld_plantCount_get
int32_t bro_flora_FloraWorld_plantCount_get(void* self);

// bro.flora.FloraWorld.prototype.prototypeCount read
//   registered at __bro_native.flora.FloraWorld_prototypeCount_get
int32_t bro_flora_FloraWorld_prototypeCount_get(void* self);

// bro.flora.FloraWorld.prototype.moduleCount read
//   registered at __bro_native.flora.FloraWorld_moduleCount_get
int32_t bro_flora_FloraWorld_moduleCount_get(void* self);

#if BRO_WITH_FLORA
// bro.flora.setWind
//   registered at __bro_native.flora.setWind
void bro_flora_setWind(double strength, double dirX, double dirY);
#endif

#if BRO_WITH_FLORA
// bro.flora.wind
//   registered at __bro_native.flora.wind
void bro_flora_wind(double strength, double dirX, double dirY);
#endif

#if BRO_WITH_FLORA
// bro.flora.setDensity
//   registered at __bro_native.flora.setDensity
void bro_flora_setDensity(double density);
#endif

#if BRO_WITH_FLORA
// bro.flora.density
//   registered at __bro_native.flora.density
void bro_flora_density(double density);
#endif

#if BRO_WITH_FLORA
// bro.flora.update
//   registered at __bro_native.flora.update
void bro_flora_update(double dt);
#endif

#if BRO_WITH_FLORA
// bro.flora.clear
//   registered at __bro_native.flora.clear
void bro_flora_clear(void);
#endif

#ifdef __cplusplus
}  // extern "C"
#endif
