// native_gesture_register.cpp — registers every native of native_gesture_decl.h with
// bronze (embed::registerNative) under __bro_native.gesture. Generated by brosurface
// (gen/emit_natives.mjs); do not edit. Call registerNatives_gesture from registerBroNatives
// (src/bronze_host/host_natives.h) on the thread that runs the program.
//
// Class members are Function-kind natives taking the handle first (the wrapper's
// receiver is dynamic, so a Method-kind registration would be unreachable from it);
// each class has one Constructor-kind registration, which mints the class, and its
// prototype is published as __bro_native.gesture.<Class>Proto for gesture.js to chain.

#include "native_gesture_decl.h"

#include "embed/embed.h"

#include <initializer_list>
#include <string>

namespace bro::bronze_host {

namespace {

namespace ev = bronze::embed;

ev::NativeSignature sig(const char* ret, std::initializer_list<const char*> params, ev::NativeKind kind) {
    ev::NativeSignature s;
    s.returnType = ret;
    for (const char* p : params) s.paramTypes.emplace_back(p);
    s.kind = kind;
    return s;
}

bool fn(const char* path, void* f, const char* ret, std::initializer_list<const char*> params, std::string* error) {
    return ev::registerNative(path, f, sig(ret, params, ev::NativeKind::Function), error);
}

bool ctor(const char* path, void* f, ev::HandleDestructor dtor, bronze::runtime::Finalize finalize,
          std::initializer_list<const char*> params, std::string* error) {
    ev::NativeSignature s = sig(path, params, ev::NativeKind::Constructor);
    s.className = path;
    s.destructor = dtor;
    s.finalize = finalize;
    return ev::registerNative(path, f, s, error);
}

// The prototype every instance of a class is born on, published where the
// wrapper reads it. Skipped when the roots are not there (bro-native-manifest
// registers without them; the manifest does not need the prototype).
void publishPrototype(const char* ns, const char* className, const char* protoName) {
    ev::GlobalValue root = ev::globalValue("__bro_native");
    if (!root.found || !ev::isObject(root.value)) return;
    ev::GlobalValue proto = ev::nativeClassPrototype(className);
    if (!proto.found) return;
    // Each value rooted before the next allocating call (bronze embed GC contract).
    ev::Persistent rootSlot(root.value);
    ev::Persistent protoSlot(proto.value);
    ev::Persistent nsSlot(ev::getProperty(rootSlot.get(), ns));
    if (!ev::isObject(nsSlot.get())) return;
    ev::setProperty(nsSlot.get(), protoName, protoSlot.get());
}

template <typename F>
void* p(F* f) { return reinterpret_cast<void*>(f); }

}  // namespace

bool registerNatives_gesture(std::string* error) {
    bool ok =
        ctor("__bro_native.gesture.GestureStreamView", p(&bro_gesture_GestureStreamView_ctor), &bro_gesture_GestureStreamView_dtor, bronze::runtime::Finalize::InSweep, {}, error) &&
        fn("__bro_native.gesture.GestureStreamView_active_get", p(&bro_gesture_GestureStreamView_active_get), "bool", {"__bro_native.gesture.GestureStreamView"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_enrollFromAudio", p(&bro_gesture_GestureStreamView_enrollFromAudio), "i32", {"__bro_native.gesture.GestureStreamView", "str", "f32[]", "f64", "f64", "f64", "f64", "i32", "i32", "i32", "i32"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_remove", p(&bro_gesture_GestureStreamView_remove), "bool", {"__bro_native.gesture.GestureStreamView", "str"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_clear", p(&bro_gesture_GestureStreamView_clear), "void", {"__bro_native.gesture.GestureStreamView"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_templates", p(&bro_gesture_GestureStreamView_templates), "str", {"__bro_native.gesture.GestureStreamView"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_inspect", p(&bro_gesture_GestureStreamView_inspect), "bool", {"__bro_native.gesture.GestureStreamView", "str"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_inspect_name", p(&bro_gesture_GestureStreamView_inspect_name), "str", {}, error) &&
        fn("__bro_native.gesture.GestureStreamView_inspect_kind", p(&bro_gesture_GestureStreamView_inspect_kind), "str", {}, error) &&
        fn("__bro_native.gesture.GestureStreamView_inspect_frameMs", p(&bro_gesture_GestureStreamView_inspect_frameMs), "f64", {}, error) &&
        fn("__bro_native.gesture.GestureStreamView_inspect_intervalsMs", p(&bro_gesture_GestureStreamView_inspect_intervalsMs), "f64[]", {}, error) &&
        fn("__bro_native.gesture.GestureStreamView_inspect_onsets", p(&bro_gesture_GestureStreamView_inspect_onsets), "str", {}, error) &&
        fn("__bro_native.gesture.GestureStreamView_inspect_toneHz", p(&bro_gesture_GestureStreamView_inspect_toneHz), "f64", {}, error) &&
        fn("__bro_native.gesture.GestureStreamView_inspect_toneMs", p(&bro_gesture_GestureStreamView_inspect_toneMs), "f64", {}, error) &&
        fn("__bro_native.gesture.GestureStreamView_inspect_toneSpread", p(&bro_gesture_GestureStreamView_inspect_toneSpread), "f64", {}, error) &&
        fn("__bro_native.gesture.GestureStreamView_reset", p(&bro_gesture_GestureStreamView_reset), "void", {"__bro_native.gesture.GestureStreamView"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_listen", p(&bro_gesture_GestureStreamView_listen), "void", {"__bro_native.gesture.GestureStreamView", "dynamic"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_stop", p(&bro_gesture_GestureStreamView_stop), "void", {"__bro_native.gesture.GestureStreamView"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_isActive", p(&bro_gesture_GestureStreamView_isActive), "bool", {"__bro_native.gesture.GestureStreamView"}, error) &&
        fn("__bro_native.gesture.GestureStreamView_sampleRate", p(&bro_gesture_GestureStreamView_sampleRate), "i32", {"__bro_native.gesture.GestureStreamView"}, error);
#if BRO_WITH_SOUNDML
    ok = ok &&
        fn("__bro_native.gesture.init", p(&bro_gesture_init), "void", {}, error) &&
        fn("__bro_native.gesture.enrollFromAudio", p(&bro_gesture_enrollFromAudio), "i32", {"str", "f32[]", "f64", "f64", "f64", "f64", "i32", "i32", "i32", "i32"}, error) &&
        fn("__bro_native.gesture.remove", p(&bro_gesture_remove), "bool", {"str"}, error) &&
        fn("__bro_native.gesture.clear", p(&bro_gesture_clear), "void", {}, error) &&
        fn("__bro_native.gesture.templates", p(&bro_gesture_templates), "str", {}, error) &&
        fn("__bro_native.gesture.inspect", p(&bro_gesture_inspect), "bool", {"str"}, error) &&
        fn("__bro_native.gesture.inspect_name", p(&bro_gesture_inspect_name), "str", {}, error) &&
        fn("__bro_native.gesture.inspect_kind", p(&bro_gesture_inspect_kind), "str", {}, error) &&
        fn("__bro_native.gesture.inspect_frameMs", p(&bro_gesture_inspect_frameMs), "f64", {}, error) &&
        fn("__bro_native.gesture.inspect_intervalsMs", p(&bro_gesture_inspect_intervalsMs), "f64[]", {}, error) &&
        fn("__bro_native.gesture.inspect_onsets", p(&bro_gesture_inspect_onsets), "str", {}, error) &&
        fn("__bro_native.gesture.inspect_toneHz", p(&bro_gesture_inspect_toneHz), "f64", {}, error) &&
        fn("__bro_native.gesture.inspect_toneMs", p(&bro_gesture_inspect_toneMs), "f64", {}, error) &&
        fn("__bro_native.gesture.inspect_toneSpread", p(&bro_gesture_inspect_toneSpread), "f64", {}, error) &&
        fn("__bro_native.gesture.reset", p(&bro_gesture_reset), "void", {}, error) &&
        fn("__bro_native.gesture.listen", p(&bro_gesture_listen), "void", {"dynamic"}, error) &&
        fn("__bro_native.gesture.stop", p(&bro_gesture_stop), "void", {}, error) &&
        fn("__bro_native.gesture.isActive", p(&bro_gesture_isActive), "bool", {}, error) &&
        fn("__bro_native.gesture.sampleRate", p(&bro_gesture_sampleRate), "i32", {}, error);
#endif
    if (!ok) return false;
    publishPrototype("gesture", "__bro_native.gesture.GestureStreamView", "GestureStreamViewProto");
    return true;
}

}  // namespace bro::bronze_host
