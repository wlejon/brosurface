// native_gesture_decl.h — the C entry points behind idl/gesture.idl, one prototype per
// native. Generated by brosurface (gen/emit_natives.mjs); do not edit. The bodies
// are hand-written in bro against the engine, and the compiler checks them here.
//
// How each type crosses (schema/native_types.mjs, bronze/src/abi/bronze_native_type.h):
//   double / int32_t / bool      f64 / i32 / bool
//   const char*                  str — UTF-8, valid for the call only; a RESULT must
//                                outlive the return (a per-thread scratch), and one
//                                marked JSON carries JSON of the IDL type in the comment
//   uint64_t                     dynamic — a callback's raw Value bits; keep it in a
//                                Persistent, never across an allocating call raw
//   (const T*, uint32_t)         a typed array: elements and count, valid for the call
//   void*                        a class handle: what the class constructor returned
//   bronze_native_buffer* out    a typed-array RESULT: the function returns void and
//                                fills *out (data, length in elements); release == NULL
//                                and the runtime copies during the call; release set
//                                ([transfer]) and the array is a view over data, with
//                                release(ctx) run when it is collected
//   bool <x>_given, T <x>        an optional value with no declared default, and
//                                whether it was passed
//
// A dictionary or list RESULT crosses as reads: the operation runs and keeps its
// result in a per-thread slot, answering nothing (or the list's length), and the
// <op>_<member> / <op>_at natives read from that slot until the next call.
#pragma once

#include <stdbool.h>
#include <stdint.h>

#include "abi/bronze_native_type.h"  // bronze_native_buffer, owned by bronze

#ifdef __cplusplus
extern "C" {
#endif

// destructor of __bro_native.gesture.GestureStreamView: frees what the constructor (or a native returning a GestureStreamView) allocated
void bro_gesture_GestureStreamView_dtor(void* self);

// constructor of __bro_native.gesture.GestureStreamView: registered so the class exists (instances come from natives returning a GestureStreamView); never called by the wrapper, the body may return NULL
//   registered at __bro_native.gesture.GestureStreamView
void* bro_gesture_GestureStreamView_ctor(void);

// bro.gesture.GestureStreamView.prototype.active read
//   registered at __bro_native.gesture.GestureStreamView_active_get
bool bro_gesture_GestureStreamView_active_get(void* self);

// bro.gesture.GestureStreamView.prototype.enrollFromAudio
//   registered at __bro_native.gesture.GestureStreamView_enrollFromAudio
int32_t bro_gesture_GestureStreamView_enrollFromAudio(void* self, const char* name, const float* samples, uint32_t samples_len, double policy_tempoTol, double policy_pitchTol, double policy_pitchStabilityTol, double policy_shapeTol, int32_t policy_refractoryFrames, int32_t policy_minOnsets, int32_t policy_minToneFrames, int32_t policy_onsetSigFrames);

// bro.gesture.GestureStreamView.prototype.remove
//   registered at __bro_native.gesture.GestureStreamView_remove
bool bro_gesture_GestureStreamView_remove(void* self, const char* name);

// bro.gesture.GestureStreamView.prototype.clear
//   registered at __bro_native.gesture.GestureStreamView_clear
void bro_gesture_GestureStreamView_clear(void* self);

// bro.gesture.GestureStreamView.prototype.templates
//   registered at __bro_native.gesture.GestureStreamView_templates
//   result: JSON of sequence<DOMString>
const char* bro_gesture_GestureStreamView_templates(void* self);

// bro.gesture.GestureStreamView.prototype.inspect: runs the operation and keeps its GestureInspection result in a per-thread slot the GestureStreamView_inspect_<member> reads answer from until the next call; false = null
//   registered at __bro_native.gesture.GestureStreamView_inspect
bool bro_gesture_GestureStreamView_inspect(void* self, const char* name);

// GestureInspection.name of the result GestureStreamView_inspect kept
//   registered at __bro_native.gesture.GestureStreamView_inspect_name
const char* bro_gesture_GestureStreamView_inspect_name(void);

// GestureInspection.kind of the result GestureStreamView_inspect kept
//   registered at __bro_native.gesture.GestureStreamView_inspect_kind
const char* bro_gesture_GestureStreamView_inspect_kind(void);

// GestureInspection.frameMs of the result GestureStreamView_inspect kept
//   registered at __bro_native.gesture.GestureStreamView_inspect_frameMs
double bro_gesture_GestureStreamView_inspect_frameMs(void);

// GestureInspection.intervalsMs of the result GestureStreamView_inspect kept
//   registered at __bro_native.gesture.GestureStreamView_inspect_intervalsMs
//   result: f64[] in *out; out->release == NULL, the runtime copies
void bro_gesture_GestureStreamView_inspect_intervalsMs(bronze_native_buffer* out);

// GestureInspection.onsets of the result GestureStreamView_inspect kept
//   registered at __bro_native.gesture.GestureStreamView_inspect_onsets
//   result: JSON of sequence<GestureOnsetSignature>
const char* bro_gesture_GestureStreamView_inspect_onsets(void);

// GestureInspection.toneHz of the result GestureStreamView_inspect kept
//   registered at __bro_native.gesture.GestureStreamView_inspect_toneHz
double bro_gesture_GestureStreamView_inspect_toneHz(void);

// GestureInspection.toneMs of the result GestureStreamView_inspect kept
//   registered at __bro_native.gesture.GestureStreamView_inspect_toneMs
double bro_gesture_GestureStreamView_inspect_toneMs(void);

// GestureInspection.toneSpread of the result GestureStreamView_inspect kept
//   registered at __bro_native.gesture.GestureStreamView_inspect_toneSpread
double bro_gesture_GestureStreamView_inspect_toneSpread(void);

// bro.gesture.GestureStreamView.prototype.reset
//   registered at __bro_native.gesture.GestureStreamView_reset
void bro_gesture_GestureStreamView_reset(void* self);

// bro.gesture.GestureStreamView.prototype.listen
//   registered at __bro_native.gesture.GestureStreamView_listen
void bro_gesture_GestureStreamView_listen(void* self, uint64_t opts_onGesture);

// bro.gesture.GestureStreamView.prototype.stop
//   registered at __bro_native.gesture.GestureStreamView_stop
void bro_gesture_GestureStreamView_stop(void* self);

// bro.gesture.GestureStreamView.prototype.isActive
//   registered at __bro_native.gesture.GestureStreamView_isActive
bool bro_gesture_GestureStreamView_isActive(void* self);

// bro.gesture.GestureStreamView.prototype.sampleRate
//   registered at __bro_native.gesture.GestureStreamView_sampleRate
int32_t bro_gesture_GestureStreamView_sampleRate(void* self);

#if BRO_WITH_SOUNDML
// bro.gesture.init
//   registered at __bro_native.gesture.init
void bro_gesture_init(void);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.enrollFromAudio
//   registered at __bro_native.gesture.enrollFromAudio
int32_t bro_gesture_enrollFromAudio(const char* name, const float* samples, uint32_t samples_len, double policy_tempoTol, double policy_pitchTol, double policy_pitchStabilityTol, double policy_shapeTol, int32_t policy_refractoryFrames, int32_t policy_minOnsets, int32_t policy_minToneFrames, int32_t policy_onsetSigFrames);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.remove
//   registered at __bro_native.gesture.remove
bool bro_gesture_remove(const char* name);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.clear
//   registered at __bro_native.gesture.clear
void bro_gesture_clear(void);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.templates
//   registered at __bro_native.gesture.templates
//   result: JSON of sequence<DOMString>
const char* bro_gesture_templates(void);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.inspect: runs the operation and keeps its GestureInspection result in a per-thread slot the inspect_<member> reads answer from until the next call; false = null
//   registered at __bro_native.gesture.inspect
bool bro_gesture_inspect(const char* name);
#endif

#if BRO_WITH_SOUNDML
// GestureInspection.name of the result inspect kept
//   registered at __bro_native.gesture.inspect_name
const char* bro_gesture_inspect_name(void);
#endif

#if BRO_WITH_SOUNDML
// GestureInspection.kind of the result inspect kept
//   registered at __bro_native.gesture.inspect_kind
const char* bro_gesture_inspect_kind(void);
#endif

#if BRO_WITH_SOUNDML
// GestureInspection.frameMs of the result inspect kept
//   registered at __bro_native.gesture.inspect_frameMs
double bro_gesture_inspect_frameMs(void);
#endif

#if BRO_WITH_SOUNDML
// GestureInspection.intervalsMs of the result inspect kept
//   registered at __bro_native.gesture.inspect_intervalsMs
//   result: f64[] in *out; out->release == NULL, the runtime copies
void bro_gesture_inspect_intervalsMs(bronze_native_buffer* out);
#endif

#if BRO_WITH_SOUNDML
// GestureInspection.onsets of the result inspect kept
//   registered at __bro_native.gesture.inspect_onsets
//   result: JSON of sequence<GestureOnsetSignature>
const char* bro_gesture_inspect_onsets(void);
#endif

#if BRO_WITH_SOUNDML
// GestureInspection.toneHz of the result inspect kept
//   registered at __bro_native.gesture.inspect_toneHz
double bro_gesture_inspect_toneHz(void);
#endif

#if BRO_WITH_SOUNDML
// GestureInspection.toneMs of the result inspect kept
//   registered at __bro_native.gesture.inspect_toneMs
double bro_gesture_inspect_toneMs(void);
#endif

#if BRO_WITH_SOUNDML
// GestureInspection.toneSpread of the result inspect kept
//   registered at __bro_native.gesture.inspect_toneSpread
double bro_gesture_inspect_toneSpread(void);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.reset
//   registered at __bro_native.gesture.reset
void bro_gesture_reset(void);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.listen
//   registered at __bro_native.gesture.listen
void bro_gesture_listen(uint64_t opts_onGesture);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.stop
//   registered at __bro_native.gesture.stop
void bro_gesture_stop(void);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.isActive
//   registered at __bro_native.gesture.isActive
bool bro_gesture_isActive(void);
#endif

#if BRO_WITH_SOUNDML
// bro.gesture.sampleRate
//   registered at __bro_native.gesture.sampleRate
int32_t bro_gesture_sampleRate(void);
#endif

#ifdef __cplusplus
}  // extern "C"
#endif
