// time.js — the public shape of bro.time, assembled over the
// natives under __bro_native.time (native_time_decl.h states each C entry point,
// native_time_register.cpp registers them). Generated by brosurface
// (gen/emit_natives.mjs) from idl/time.idl; do not edit.
//
// Every native is spelled by its full dotted path at the point of use: that is the
// spelling the compiler lowers to a direct call. The roots this module reads as bare
// identifiers are listed in module.globals beside it.
(function () {
    'use strict';

    const accessor = (obj, name, get, set) =>
        Object.defineProperty(obj, name, { get, set, enumerable: true, configurable: true });
    const fn = (obj, name, value) =>
        Object.defineProperty(obj, name, { value, writable: true, enumerable: true, configurable: true });
    const mount = (root, name) => root[name] !== undefined ? root[name] : (root[name] = {});

    // ---- bro.time ------------------------------------------------------------
    const ns_time = mount(globalThis.bro, "time");
    accessor(ns_time, "scale",
        function () {
            return __bro_native.time.scale;
        },
        function (v) {
            __bro_native.time.scale = v;
        });
    accessor(ns_time, "paused",
        function () {
            return __bro_native.time.paused;
        },
        function (v) {
            __bro_native.time.paused = v;
        });
    accessor(ns_time, "now",
        function () {
            return __bro_native.time.now;
        },
        undefined);
})();
