// native_window_decl.h — the C entry points behind idl/window.idl, one prototype per
// native. Generated by brosurface (gen/emit_natives.mjs); do not edit. The bodies
// are hand-written in bro against the engine, and the compiler checks them here.
//
// How each type crosses (schema/native_types.mjs, bronze/src/abi/bronze_native_type.h):
//   double / int32_t / bool      f64 / i32 / bool
//   const char*                  str — UTF-8, valid for the call only; a RESULT must
//                                outlive the return (a per-thread scratch), and one
//                                marked JSON carries JSON of the IDL type in the comment
//   uint64_t                     dynamic — a callback's raw Value bits; keep it in a
//                                Persistent, never across an allocating call raw
//   (const T*, uint32_t)         a typed array: elements and count, valid for the call
//   void*                        a class handle: what the class constructor returned
//   bronze_native_buffer* out    a typed-array RESULT: the function returns void and
//                                fills *out (data, length in elements); release == NULL
//                                and the runtime copies during the call; release set
//                                ([transfer]) and the array is a view over data, with
//                                release(ctx) run when it is collected
//   bool <x>_given, T <x>        an optional value with no declared default, and
//                                whether it was passed
//
// A dictionary or list RESULT crosses as reads: the operation runs and keeps its
// result in a per-thread slot, answering nothing (or the list's length), and the
// <op>_<member> / <op>_at natives read from that slot until the next call.
#pragma once

#include <stdbool.h>
#include <stdint.h>

#include "abi/bronze_native_type.h"  // bronze_native_buffer, owned by bronze

#ifdef __cplusplus
extern "C" {
#endif

// bro.window.state read
//   registered as a namespace property (getter) at __bro_native.window.state
const char* bro_window_state_get(void);

// bro.window.borderless read
//   registered as a namespace property (getter) at __bro_native.window.borderless
bool bro_window_borderless_get(void);

// bro.window.borderless write
//   registered as a namespace property (setter) at __bro_native.window.borderless
void bro_window_borderless_set(bool v);

// bro.window.alwaysOnTop read
//   registered as a namespace property (getter) at __bro_native.window.alwaysOnTop
bool bro_window_alwaysOnTop_get(void);

// bro.window.alwaysOnTop write
//   registered as a namespace property (setter) at __bro_native.window.alwaysOnTop
void bro_window_alwaysOnTop_set(bool v);

// bro.window.minimize
//   registered at __bro_native.window.minimize
void bro_window_minimize(void);

// bro.window.maximize
//   registered at __bro_native.window.maximize
void bro_window_maximize(void);

// bro.window.restore
//   registered at __bro_native.window.restore
void bro_window_restore(void);

// bro.window.getPosition: runs the operation and keeps its WindowPosition result in a per-thread slot the getPosition_<member> reads answer from until the next call
//   registered at __bro_native.window.getPosition
void bro_window_getPosition(void);

// WindowPosition.x of the result getPosition kept
//   registered at __bro_native.window.getPosition_x
int32_t bro_window_getPosition_x(void);

// WindowPosition.y of the result getPosition kept
//   registered at __bro_native.window.getPosition_y
int32_t bro_window_getPosition_y(void);

// bro.window.setPosition
//   registered at __bro_native.window.setPosition
void bro_window_setPosition(int32_t x, int32_t y);

// bro.window.getMinSize: runs the operation and keeps its WindowSize result in a per-thread slot the getMinSize_<member> reads answer from until the next call
//   registered at __bro_native.window.getMinSize
void bro_window_getMinSize(void);

// WindowSize.width of the result getMinSize kept
//   registered at __bro_native.window.getMinSize_width
int32_t bro_window_getMinSize_width(void);

// WindowSize.height of the result getMinSize kept
//   registered at __bro_native.window.getMinSize_height
int32_t bro_window_getMinSize_height(void);

// bro.window.setMinSize
//   registered at __bro_native.window.setMinSize
void bro_window_setMinSize(int32_t width, int32_t height);

// bro.window.getMaxSize: runs the operation and keeps its WindowSize result in a per-thread slot the getMaxSize_<member> reads answer from until the next call
//   registered at __bro_native.window.getMaxSize
void bro_window_getMaxSize(void);

// WindowSize.width of the result getMaxSize kept
//   registered at __bro_native.window.getMaxSize_width
int32_t bro_window_getMaxSize_width(void);

// WindowSize.height of the result getMaxSize kept
//   registered at __bro_native.window.getMaxSize_height
int32_t bro_window_getMaxSize_height(void);

// bro.window.setMaxSize
//   registered at __bro_native.window.setMaxSize
void bro_window_setMaxSize(int32_t width, int32_t height);

// bro.window.getDisplays: runs the operation, keeps the list in a per-thread slot, and answers its length; getDisplays_<member>(index) reads from it
//   registered at __bro_native.window.getDisplays
int32_t bro_window_getDisplays(void);

// DisplayInfo.id of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_id
double bro_window_getDisplays_id(int32_t index);

// DisplayInfo.name of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_name
const char* bro_window_getDisplays_name(int32_t index);

// DisplayInfo.x of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_x
int32_t bro_window_getDisplays_x(int32_t index);

// DisplayInfo.y of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_y
int32_t bro_window_getDisplays_y(int32_t index);

// DisplayInfo.width of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_width
int32_t bro_window_getDisplays_width(int32_t index);

// DisplayInfo.height of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_height
int32_t bro_window_getDisplays_height(int32_t index);

// DisplayInfo.workX of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_workX
int32_t bro_window_getDisplays_workX(int32_t index);

// DisplayInfo.workY of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_workY
int32_t bro_window_getDisplays_workY(int32_t index);

// DisplayInfo.workWidth of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_workWidth
int32_t bro_window_getDisplays_workWidth(int32_t index);

// DisplayInfo.workHeight of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_workHeight
int32_t bro_window_getDisplays_workHeight(int32_t index);

// DisplayInfo.refreshRate of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_refreshRate
double bro_window_getDisplays_refreshRate(int32_t index);

// DisplayInfo.contentScale of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_contentScale
double bro_window_getDisplays_contentScale(int32_t index);

// DisplayInfo.isPrimary of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_isPrimary
bool bro_window_getDisplays_isPrimary(int32_t index);

// DisplayInfo.isCurrent of the result getDisplays kept, at [index]
//   registered at __bro_native.window.getDisplays_isCurrent
bool bro_window_getDisplays_isCurrent(int32_t index);

// bro.window.moveToDisplay
//   registered at __bro_native.window.moveToDisplay
bool bro_window_moveToDisplay(double id);

#ifdef __cplusplus
}  // extern "C"
#endif
