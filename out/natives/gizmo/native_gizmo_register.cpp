// native_gizmo_register.cpp — registers every native of native_gizmo_decl.h with
// bronze (embed::registerNative) under __bro_native.gizmo. Generated by brosurface
// (gen/emit_natives.mjs); do not edit. Call registerNatives_gizmo from registerBroNatives
// (src/bronze_host/host_natives.h) on the thread that runs the program.
//
// Class members are Function-kind natives taking the handle first (the wrapper's
// receiver is dynamic, so a Method-kind registration would be unreachable from it);
// each class has one Constructor-kind registration, which mints the class, and its
// prototype is published as __bro_native.gizmo.<Class>Proto for gizmo.js to chain.

#include "native_gizmo_decl.h"

#if BRO_WITH_3D

#include "embed/embed.h"

#include <initializer_list>
#include <string>

namespace bro::bronze_host {

namespace {

namespace ev = bronze::embed;

ev::NativeSignature sig(const char* ret, std::initializer_list<const char*> params, ev::NativeKind kind) {
    ev::NativeSignature s;
    s.returnType = ret;
    for (const char* p : params) s.paramTypes.emplace_back(p);
    s.kind = kind;
    return s;
}

bool fn(const char* path, void* f, const char* ret, std::initializer_list<const char*> params, std::string* error) {
    return ev::registerNative(path, f, sig(ret, params, ev::NativeKind::Function), error);
}

bool getter(const char* path, void* f, const char* ret, std::string* error) {
    return ev::registerNative(path, f, sig(ret, {}, ev::NativeKind::Getter), error);
}

template <typename F>
void* p(F* f) { return reinterpret_cast<void*>(f); }

}  // namespace

bool registerNatives_gizmo(std::string* error) {
    const bool ok =
        getter("__bro_native.gizmo.visible", p(&bro_gizmo_visible_get), "bool", error) &&
        getter("__bro_native.gizmo.dragging", p(&bro_gizmo_dragging_get), "bool", error) &&
        fn("__bro_native.gizmo.show", p(&bro_gizmo_show), "void", {}, error) &&
        fn("__bro_native.gizmo.hide", p(&bro_gizmo_hide), "void", {}, error) &&
        fn("__bro_native.gizmo.setMode", p(&bro_gizmo_setMode), "void", {"str"}, error) &&
        fn("__bro_native.gizmo.setSpace", p(&bro_gizmo_setSpace), "void", {"str"}, error) &&
        fn("__bro_native.gizmo.setPosition", p(&bro_gizmo_setPosition), "void", {"f64", "f64", "f64"}, error) &&
        fn("__bro_native.gizmo.setOrientation", p(&bro_gizmo_setOrientation), "void", {"f64", "f64", "f64", "f64"}, error) &&
        fn("__bro_native.gizmo.configure", p(&bro_gizmo_configure), "void", {"bool", "f64", "bool", "str", "bool", "str", "bool", "str", "bool", "str", "bool", "str", "bool", "f64", "bool", "f64", "bool", "bool"}, error) &&
        fn("__bro_native.gizmo.attach", p(&bro_gizmo_attach), "void", {"dynamic", "dynamic", "dynamic", "dynamic", "dynamic", "dynamic", "dynamic", "dynamic"}, error) &&
        fn("__bro_native.gizmo.detach", p(&bro_gizmo_detach), "void", {}, error);
    if (!ok) return false;
    return true;
}

}  // namespace bro::bronze_host

#else  // !(BRO_WITH_3D)

#include <string>

namespace bro::bronze_host {

// Compiled out: nothing is registered and gizmo.js is not to be installed.
bool registerNatives_gizmo(std::string*) { return true; }

}  // namespace bro::bronze_host

#endif  // BRO_WITH_3D
