// native_gizmo_decl.h — the C entry points behind idl/gizmo.idl, one prototype per
// native. Generated by brosurface (gen/emit_natives.mjs); do not edit. The bodies
// are hand-written in bro against the engine, and the compiler checks them here.
//
// How each type crosses (schema/native_types.mjs, bronze/src/abi/bronze_native_type.h):
//   double / int32_t / bool      f64 / i32 / bool
//   const char*                  str — UTF-8, valid for the call only; a RESULT must
//                                outlive the return (a per-thread scratch), and one
//                                marked JSON carries JSON of the IDL type in the comment
//   uint64_t                     dynamic — a callback's raw Value bits; keep it in a
//                                Persistent, never across an allocating call raw
//   (const T*, uint32_t)         a typed array: elements and count, valid for the call
//   void*                        a class handle: what the class constructor returned
//   bronze_native_buffer* out    a typed-array RESULT: the function returns void and
//                                fills *out (data, length in elements); release == NULL
//                                and the runtime copies during the call; release set
//                                ([transfer]) and the array is a view over data, with
//                                release(ctx) run when it is collected
//   bool <x>_given, T <x>        an optional value with no declared default, and
//                                whether it was passed
//
// A dictionary or list RESULT crosses as reads: the operation runs and keeps its
// result in a per-thread slot, answering nothing (or the list's length), and the
// <op>_<member> / <op>_at natives read from that slot until the next call.
//
// Gate: BRO_WITH_3D (native_gizmo_register.cpp registers nothing when it is off).
#pragma once

#include <stdbool.h>
#include <stdint.h>

#include "abi/bronze_native_type.h"  // bronze_native_buffer, owned by bronze

#ifdef __cplusplus
extern "C" {
#endif

// bro.gizmo.visible read
//   registered as a namespace property (getter) at __bro_native.gizmo.visible
bool bro_gizmo_visible_get(void);

// bro.gizmo.dragging read
//   registered as a namespace property (getter) at __bro_native.gizmo.dragging
bool bro_gizmo_dragging_get(void);

// bro.gizmo.show
//   registered at __bro_native.gizmo.show
void bro_gizmo_show(void);

// bro.gizmo.hide
//   registered at __bro_native.gizmo.hide
void bro_gizmo_hide(void);

// bro.gizmo.setMode
//   registered at __bro_native.gizmo.setMode
void bro_gizmo_setMode(const char* mode);

// bro.gizmo.setSpace
//   registered at __bro_native.gizmo.setSpace
void bro_gizmo_setSpace(const char* space);

// bro.gizmo.setPosition
//   registered at __bro_native.gizmo.setPosition
void bro_gizmo_setPosition(double x, double y, double z);

// bro.gizmo.setOrientation
//   registered at __bro_native.gizmo.setOrientation
void bro_gizmo_setOrientation(double x, double y, double z, double w);

// bro.gizmo.configure
//   registered at __bro_native.gizmo.configure
//   config_size_given, config_colors_x_given, config_colors_y_given, config_colors_z_given, config_colors_hover_given, config_colors_active_given, config_emissive_given, config_emissiveHover_given, config_alwaysOnTop_given: false when the value was not passed (no declared default)
void bro_gizmo_configure(bool config_size_given, double config_size, bool config_colors_x_given, const char* config_colors_x, bool config_colors_y_given, const char* config_colors_y, bool config_colors_z_given, const char* config_colors_z, bool config_colors_hover_given, const char* config_colors_hover, bool config_colors_active_given, const char* config_colors_active, bool config_emissive_given, double config_emissive, bool config_emissiveHover_given, double config_emissiveHover, bool config_alwaysOnTop_given, bool config_alwaysOnTop);

// bro.gizmo.attach
//   registered at __bro_native.gizmo.attach
void bro_gizmo_attach(uint64_t handlers_position, uint64_t handlers_orientation, uint64_t handlers_beginDrag, uint64_t handlers_translate, uint64_t handlers_rotate, uint64_t handlers_scale, uint64_t handlers_endDrag, uint64_t handlers_hoverChange);

// bro.gizmo.detach
//   registered at __bro_native.gizmo.detach
void bro_gizmo_detach(void);

#ifdef __cplusplus
}  // extern "C"
#endif
