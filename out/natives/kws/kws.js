// kws.js — the public shape of bro.kws, bro.kws.KwsStreamView, assembled over the
// natives under __bro_native.kws (native_kws_decl.h states each C entry point,
// native_kws_register.cpp registers them). Generated by brosurface
// (gen/emit_natives.mjs) from idl/kws.idl; do not edit.
//
// Every native is spelled by its full dotted path at the point of use: that is the
// spelling the compiler lowers to a direct call. The roots this module reads as bare
// identifiers are listed in module.globals beside it.
//
// [manual] members (no native, installed by hand-written JS after this module):
//   bro.kws.KwsStreamView.prototype.feed
//   bro.kws.feed
(function () {
    'use strict';

    const accessor = (obj, name, get, set) =>
        Object.defineProperty(obj, name, { get, set, enumerable: true, configurable: true });
    const fn = (obj, name, value) =>
        Object.defineProperty(obj, name, { value, writable: true, enumerable: true, configurable: true });
    const EMPTY_F32 = new Float32Array(0);
    const EMPTY_I32 = new Int32Array(0);
    const mount = (root, name) => root[name] !== undefined ? root[name] : (root[name] = {});
    const toF32 = (v) => v instanceof Float32Array ? v : Float32Array.from(v);
    const toI32 = (v) => v instanceof Int32Array ? v : Int32Array.from(v);

    // ---- bro.kws -------------------------------------------------------------
    const ns_kws = mount(bro, "kws");
    fn(ns_kws, "init", function init() {
        __bro_native.kws.init();
    });
    fn(ns_kws, "load", function load(opts) {
        if (opts === undefined) throw new TypeError("bro.kws.load: opts is required");
        const d_opts = opts;
        const d_opts_smoothing = d_opts.smoothing === undefined ? {} : d_opts.smoothing;
        __bro_native.kws.load(d_opts.weights !== undefined, d_opts.weights === undefined ? '' : d_opts.weights, d_opts.device !== undefined, d_opts.device === undefined ? '' : d_opts.device, d_opts.threshold === undefined ? 0.4 : d_opts.threshold, d_opts.refractoryMs === undefined ? 600 : d_opts.refractoryMs, d_opts_smoothing.hits !== undefined, d_opts_smoothing.hits === undefined ? 0 : d_opts_smoothing.hits, d_opts_smoothing.window !== undefined, d_opts_smoothing.window === undefined ? 0 : d_opts_smoothing.window, d_opts.minPhonemes === undefined ? 3 : d_opts.minPhonemes, d_opts.entrySilenceFrames === undefined ? 2 : d_opts.entrySilenceFrames, d_opts.emissionFloor === undefined ? 0.15 : d_opts.emissionFloor, d_opts.minCoverage === undefined ? 0 : d_opts.minCoverage, d_opts.scoreNorm === undefined ? 0 : d_opts.scoreNorm, d_opts.enrollGaps === undefined ? false : d_opts.enrollGaps, d_opts.gapMinFrames === undefined ? 5 : d_opts.gapMinFrames, d_opts.gapTolerance === undefined ? 0.5 : d_opts.gapTolerance);
    });
    fn(ns_kws, "unload", function unload() {
        __bro_native.kws.unload();
    });
    fn(ns_kws, "enroll", function enroll(name, phonemeIds, policy) {
        if (name === undefined) throw new TypeError("bro.kws.enroll: name is required");
        if (phonemeIds === undefined) throw new TypeError("bro.kws.enroll: phonemeIds is required");
        const d_policy = policy === undefined ? {} : policy;
        const d_policy_smoothing = d_policy.smoothing === undefined ? {} : d_policy.smoothing;
        return __bro_native.kws.enroll(name, toI32(phonemeIds), d_policy.weights !== undefined, d_policy.weights === undefined ? '' : d_policy.weights, d_policy.device !== undefined, d_policy.device === undefined ? '' : d_policy.device, d_policy.threshold === undefined ? 0.4 : d_policy.threshold, d_policy.refractoryMs === undefined ? 600 : d_policy.refractoryMs, d_policy_smoothing.hits !== undefined, d_policy_smoothing.hits === undefined ? 0 : d_policy_smoothing.hits, d_policy_smoothing.window !== undefined, d_policy_smoothing.window === undefined ? 0 : d_policy_smoothing.window, d_policy.minPhonemes === undefined ? 3 : d_policy.minPhonemes, d_policy.entrySilenceFrames === undefined ? 2 : d_policy.entrySilenceFrames, d_policy.emissionFloor === undefined ? 0.15 : d_policy.emissionFloor, d_policy.minCoverage === undefined ? 0 : d_policy.minCoverage, d_policy.scoreNorm === undefined ? 0 : d_policy.scoreNorm, d_policy.enrollGaps === undefined ? false : d_policy.enrollGaps, d_policy.gapMinFrames === undefined ? 5 : d_policy.gapMinFrames, d_policy.gapTolerance === undefined ? 0.5 : d_policy.gapTolerance);
    });
    fn(ns_kws, "enrollFromAudio", function enrollFromAudio(name, samples, policy) {
        if (name === undefined) throw new TypeError("bro.kws.enrollFromAudio: name is required");
        if (samples === undefined) throw new TypeError("bro.kws.enrollFromAudio: samples is required");
        const d_policy = policy === undefined ? {} : policy;
        const d_policy_smoothing = d_policy.smoothing === undefined ? {} : d_policy.smoothing;
        return __bro_native.kws.enrollFromAudio(name, toF32(samples), d_policy.weights !== undefined, d_policy.weights === undefined ? '' : d_policy.weights, d_policy.device !== undefined, d_policy.device === undefined ? '' : d_policy.device, d_policy.threshold === undefined ? 0.4 : d_policy.threshold, d_policy.refractoryMs === undefined ? 600 : d_policy.refractoryMs, d_policy_smoothing.hits !== undefined, d_policy_smoothing.hits === undefined ? 0 : d_policy_smoothing.hits, d_policy_smoothing.window !== undefined, d_policy_smoothing.window === undefined ? 0 : d_policy_smoothing.window, d_policy.minPhonemes === undefined ? 3 : d_policy.minPhonemes, d_policy.entrySilenceFrames === undefined ? 2 : d_policy.entrySilenceFrames, d_policy.emissionFloor === undefined ? 0.15 : d_policy.emissionFloor, d_policy.minCoverage === undefined ? 0 : d_policy.minCoverage, d_policy.scoreNorm === undefined ? 0 : d_policy.scoreNorm, d_policy.enrollGaps === undefined ? false : d_policy.enrollGaps, d_policy.gapMinFrames === undefined ? 5 : d_policy.gapMinFrames, d_policy.gapTolerance === undefined ? 0.5 : d_policy.gapTolerance);
    });
    fn(ns_kws, "enrollFromClasses", function enrollFromClasses(name, classIds, policy) {
        if (name === undefined) throw new TypeError("bro.kws.enrollFromClasses: name is required");
        if (classIds === undefined) throw new TypeError("bro.kws.enrollFromClasses: classIds is required");
        const d_policy = policy === undefined ? {} : policy;
        const d_policy_smoothing = d_policy.smoothing === undefined ? {} : d_policy.smoothing;
        return __bro_native.kws.enrollFromClasses(name, toI32(classIds), d_policy.weights !== undefined, d_policy.weights === undefined ? '' : d_policy.weights, d_policy.device !== undefined, d_policy.device === undefined ? '' : d_policy.device, d_policy.threshold === undefined ? 0.4 : d_policy.threshold, d_policy.refractoryMs === undefined ? 600 : d_policy.refractoryMs, d_policy_smoothing.hits !== undefined, d_policy_smoothing.hits === undefined ? 0 : d_policy_smoothing.hits, d_policy_smoothing.window !== undefined, d_policy_smoothing.window === undefined ? 0 : d_policy_smoothing.window, d_policy.minPhonemes === undefined ? 3 : d_policy.minPhonemes, d_policy.entrySilenceFrames === undefined ? 2 : d_policy.entrySilenceFrames, d_policy.emissionFloor === undefined ? 0.15 : d_policy.emissionFloor, d_policy.minCoverage === undefined ? 0 : d_policy.minCoverage, d_policy.scoreNorm === undefined ? 0 : d_policy.scoreNorm, d_policy.enrollGaps === undefined ? false : d_policy.enrollGaps, d_policy.gapMinFrames === undefined ? 5 : d_policy.gapMinFrames, d_policy.gapTolerance === undefined ? 0.5 : d_policy.gapTolerance);
    });
    fn(ns_kws, "inspect", function inspect(name) {
        if (name === undefined) throw new TypeError("bro.kws.inspect: name is required");
        if (!__bro_native.kws.inspect(name)) return null;
        return { name: __bro_native.kws.inspect_name(), threshold: __bro_native.kws.inspect_threshold(), frameMs: __bro_native.kws.inspect_frameMs(), hasGaps: __bro_native.kws.inspect_hasGaps(), states: JSON.parse(__bro_native.kws.inspect_states()) };
    });
    fn(ns_kws, "remove", function remove(name) {
        if (name === undefined) throw new TypeError("bro.kws.remove: name is required");
        return __bro_native.kws.remove(name);
    });
    fn(ns_kws, "clear", function clear() {
        __bro_native.kws.clear();
    });
    fn(ns_kws, "templates", function templates() {
        return JSON.parse(__bro_native.kws.templates());
    });
    fn(ns_kws, "reset", function reset() {
        __bro_native.kws.reset();
    });
    fn(ns_kws, "listen", function listen(opts) {
        if (opts === undefined) throw new TypeError("bro.kws.listen: opts is required");
        const d_opts = opts;
        if (d_opts.onSpot === undefined) throw new TypeError("bro.kws.listen: opts.onSpot is required");
        __bro_native.kws.listen(d_opts.onSpot);
    });
    fn(ns_kws, "stop", function stop() {
        __bro_native.kws.stop();
    });
    fn(ns_kws, "suspend", function suspend() {
        __bro_native.kws.suspend();
    });
    fn(ns_kws, "resume", function resume() {
        __bro_native.kws.resume();
    });
    fn(ns_kws, "isActive", function isActive() {
        return __bro_native.kws.isActive();
    });
    fn(ns_kws, "isSuspended", function isSuspended() {
        return __bro_native.kws.isSuspended();
    });
    fn(ns_kws, "isLoaded", function isLoaded() {
        return __bro_native.kws.isLoaded();
    });
    fn(ns_kws, "sampleRate", function sampleRate() {
        return __bro_native.kws.sampleRate();
    });
    fn(ns_kws, "prefixProgress", function prefixProgress() {
        return __bro_native.kws.prefixProgress();
    });
    fn(ns_kws, "progress", function progress() {
        if (!__bro_native.kws.progress()) return null;
        return { frames: __bro_native.kws.progress_frames(), generation: __bro_native.kws.progress_generation(), templates: JSON.parse(__bro_native.kws.progress_templates()) };
    });
    fn(ns_kws, "posterior", function posterior(topK) {
        if (!__bro_native.kws.posterior(topK === undefined ? 3 : topK)) return null;
        return { frame: __bro_native.kws.posterior_frame(), top: JSON.parse(__bro_native.kws.posterior_top()) };
    });
    fn(ns_kws, "stats", function stats() {
        if (!__bro_native.kws.stats()) return null;
        return { framesDelivered: __bro_native.kws.stats_framesDelivered(), samplesDelivered: __bro_native.kws.stats_samplesDelivered(), rollingPeak: __bro_native.kws.stats_rollingPeak() };
    });
    // [manual] bro.kws.feed: operation (samples) — no native is generated; hand-written JS
    // installs it on ns_kws after this module has run.

    // ---- bro.kws.KwsStreamView -----------------------------------------------
    function KwsStreamView() {
        throw new TypeError("bro.kws.KwsStreamView is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.kws.KwsStreamViewProto;
        if (proto === undefined) throw new Error("bro.kws.KwsStreamView: native class prototype not published (registerNatives_kws did not run)");
        Object.setPrototypeOf(proto, KwsStreamView.prototype);
    }
    fn(mount(bro, "kws"), "KwsStreamView", KwsStreamView);
    accessor(KwsStreamView.prototype, "active",
        function () {
            return __bro_native.kws.KwsStreamView_active_get(this);
        },
        undefined);
    fn(KwsStreamView.prototype, "enroll", function enroll(name, phonemeIds, policy) {
        if (name === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.enroll: name is required");
        if (phonemeIds === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.enroll: phonemeIds is required");
        const d_policy = policy === undefined ? {} : policy;
        const d_policy_smoothing = d_policy.smoothing === undefined ? {} : d_policy.smoothing;
        return __bro_native.kws.KwsStreamView_enroll(this, name, toI32(phonemeIds), d_policy.weights !== undefined, d_policy.weights === undefined ? '' : d_policy.weights, d_policy.device !== undefined, d_policy.device === undefined ? '' : d_policy.device, d_policy.threshold === undefined ? 0.4 : d_policy.threshold, d_policy.refractoryMs === undefined ? 600 : d_policy.refractoryMs, d_policy_smoothing.hits !== undefined, d_policy_smoothing.hits === undefined ? 0 : d_policy_smoothing.hits, d_policy_smoothing.window !== undefined, d_policy_smoothing.window === undefined ? 0 : d_policy_smoothing.window, d_policy.minPhonemes === undefined ? 3 : d_policy.minPhonemes, d_policy.entrySilenceFrames === undefined ? 2 : d_policy.entrySilenceFrames, d_policy.emissionFloor === undefined ? 0.15 : d_policy.emissionFloor, d_policy.minCoverage === undefined ? 0 : d_policy.minCoverage, d_policy.scoreNorm === undefined ? 0 : d_policy.scoreNorm, d_policy.enrollGaps === undefined ? false : d_policy.enrollGaps, d_policy.gapMinFrames === undefined ? 5 : d_policy.gapMinFrames, d_policy.gapTolerance === undefined ? 0.5 : d_policy.gapTolerance);
    });
    fn(KwsStreamView.prototype, "enrollFromAudio", function enrollFromAudio(name, samples, policy) {
        if (name === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.enrollFromAudio: name is required");
        if (samples === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.enrollFromAudio: samples is required");
        const d_policy = policy === undefined ? {} : policy;
        const d_policy_smoothing = d_policy.smoothing === undefined ? {} : d_policy.smoothing;
        return __bro_native.kws.KwsStreamView_enrollFromAudio(this, name, toF32(samples), d_policy.weights !== undefined, d_policy.weights === undefined ? '' : d_policy.weights, d_policy.device !== undefined, d_policy.device === undefined ? '' : d_policy.device, d_policy.threshold === undefined ? 0.4 : d_policy.threshold, d_policy.refractoryMs === undefined ? 600 : d_policy.refractoryMs, d_policy_smoothing.hits !== undefined, d_policy_smoothing.hits === undefined ? 0 : d_policy_smoothing.hits, d_policy_smoothing.window !== undefined, d_policy_smoothing.window === undefined ? 0 : d_policy_smoothing.window, d_policy.minPhonemes === undefined ? 3 : d_policy.minPhonemes, d_policy.entrySilenceFrames === undefined ? 2 : d_policy.entrySilenceFrames, d_policy.emissionFloor === undefined ? 0.15 : d_policy.emissionFloor, d_policy.minCoverage === undefined ? 0 : d_policy.minCoverage, d_policy.scoreNorm === undefined ? 0 : d_policy.scoreNorm, d_policy.enrollGaps === undefined ? false : d_policy.enrollGaps, d_policy.gapMinFrames === undefined ? 5 : d_policy.gapMinFrames, d_policy.gapTolerance === undefined ? 0.5 : d_policy.gapTolerance);
    });
    fn(KwsStreamView.prototype, "enrollFromClasses", function enrollFromClasses(name, classIds, policy) {
        if (name === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.enrollFromClasses: name is required");
        if (classIds === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.enrollFromClasses: classIds is required");
        const d_policy = policy === undefined ? {} : policy;
        const d_policy_smoothing = d_policy.smoothing === undefined ? {} : d_policy.smoothing;
        return __bro_native.kws.KwsStreamView_enrollFromClasses(this, name, toI32(classIds), d_policy.weights !== undefined, d_policy.weights === undefined ? '' : d_policy.weights, d_policy.device !== undefined, d_policy.device === undefined ? '' : d_policy.device, d_policy.threshold === undefined ? 0.4 : d_policy.threshold, d_policy.refractoryMs === undefined ? 600 : d_policy.refractoryMs, d_policy_smoothing.hits !== undefined, d_policy_smoothing.hits === undefined ? 0 : d_policy_smoothing.hits, d_policy_smoothing.window !== undefined, d_policy_smoothing.window === undefined ? 0 : d_policy_smoothing.window, d_policy.minPhonemes === undefined ? 3 : d_policy.minPhonemes, d_policy.entrySilenceFrames === undefined ? 2 : d_policy.entrySilenceFrames, d_policy.emissionFloor === undefined ? 0.15 : d_policy.emissionFloor, d_policy.minCoverage === undefined ? 0 : d_policy.minCoverage, d_policy.scoreNorm === undefined ? 0 : d_policy.scoreNorm, d_policy.enrollGaps === undefined ? false : d_policy.enrollGaps, d_policy.gapMinFrames === undefined ? 5 : d_policy.gapMinFrames, d_policy.gapTolerance === undefined ? 0.5 : d_policy.gapTolerance);
    });
    fn(KwsStreamView.prototype, "inspect", function inspect(name) {
        if (name === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.inspect: name is required");
        if (!__bro_native.kws.KwsStreamView_inspect(this, name)) return null;
        return { name: __bro_native.kws.KwsStreamView_inspect_name(), threshold: __bro_native.kws.KwsStreamView_inspect_threshold(), frameMs: __bro_native.kws.KwsStreamView_inspect_frameMs(), hasGaps: __bro_native.kws.KwsStreamView_inspect_hasGaps(), states: JSON.parse(__bro_native.kws.KwsStreamView_inspect_states()) };
    });
    fn(KwsStreamView.prototype, "remove", function remove(name) {
        if (name === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.remove: name is required");
        return __bro_native.kws.KwsStreamView_remove(this, name);
    });
    fn(KwsStreamView.prototype, "clear", function clear() {
        __bro_native.kws.KwsStreamView_clear(this);
    });
    fn(KwsStreamView.prototype, "templates", function templates() {
        return JSON.parse(__bro_native.kws.KwsStreamView_templates(this));
    });
    fn(KwsStreamView.prototype, "reset", function reset() {
        __bro_native.kws.KwsStreamView_reset(this);
    });
    fn(KwsStreamView.prototype, "listen", function listen(opts) {
        if (opts === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.listen: opts is required");
        const d_opts = opts;
        if (d_opts.onSpot === undefined) throw new TypeError("bro.kws.KwsStreamView.prototype.listen: opts.onSpot is required");
        __bro_native.kws.KwsStreamView_listen(this, d_opts.onSpot);
    });
    fn(KwsStreamView.prototype, "stop", function stop() {
        __bro_native.kws.KwsStreamView_stop(this);
    });
    fn(KwsStreamView.prototype, "suspend", function suspend() {
        __bro_native.kws.KwsStreamView_suspend(this);
    });
    fn(KwsStreamView.prototype, "resume", function resume() {
        __bro_native.kws.KwsStreamView_resume(this);
    });
    fn(KwsStreamView.prototype, "isActive", function isActive() {
        return __bro_native.kws.KwsStreamView_isActive(this);
    });
    fn(KwsStreamView.prototype, "isSuspended", function isSuspended() {
        return __bro_native.kws.KwsStreamView_isSuspended(this);
    });
    fn(KwsStreamView.prototype, "isLoaded", function isLoaded() {
        return __bro_native.kws.KwsStreamView_isLoaded(this);
    });
    fn(KwsStreamView.prototype, "sampleRate", function sampleRate() {
        return __bro_native.kws.KwsStreamView_sampleRate(this);
    });
    fn(KwsStreamView.prototype, "prefixProgress", function prefixProgress() {
        return __bro_native.kws.KwsStreamView_prefixProgress(this);
    });
    fn(KwsStreamView.prototype, "progress", function progress() {
        if (!__bro_native.kws.KwsStreamView_progress(this)) return null;
        return { frames: __bro_native.kws.KwsStreamView_progress_frames(), generation: __bro_native.kws.KwsStreamView_progress_generation(), templates: JSON.parse(__bro_native.kws.KwsStreamView_progress_templates()) };
    });
    fn(KwsStreamView.prototype, "posterior", function posterior(topK) {
        if (!__bro_native.kws.KwsStreamView_posterior(this, topK === undefined ? 3 : topK)) return null;
        return { frame: __bro_native.kws.KwsStreamView_posterior_frame(), top: JSON.parse(__bro_native.kws.KwsStreamView_posterior_top()) };
    });
    fn(KwsStreamView.prototype, "stats", function stats() {
        if (!__bro_native.kws.KwsStreamView_stats(this)) return null;
        return { framesDelivered: __bro_native.kws.KwsStreamView_stats_framesDelivered(), samplesDelivered: __bro_native.kws.KwsStreamView_stats_samplesDelivered(), rollingPeak: __bro_native.kws.KwsStreamView_stats_rollingPeak() };
    });
    // [manual] bro.kws.KwsStreamView.prototype.feed: operation (samples) — no native is generated; hand-written JS
    // installs it on KwsStreamView.prototype after this module has run.
})();
