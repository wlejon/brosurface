// rave.js — the public shape of bro.rave, bro.rave.Rave, assembled over the
// natives under __bro_native.rave (native_rave_decl.h states each C entry point,
// native_rave_register.cpp registers them). Generated by brosurface
// (gen/emit_natives.mjs) from idl/rave.idl; do not edit.
//
// Every native is spelled by its full dotted path at the point of use: that is the
// spelling the compiler lowers to a direct call. The roots this module reads as bare
// identifiers are listed in module.globals beside it.
(function () {
    'use strict';

    const accessor = (obj, name, get, set) =>
        Object.defineProperty(obj, name, { get, set, enumerable: true, configurable: true });
    const fn = (obj, name, value) =>
        Object.defineProperty(obj, name, { value, writable: true, enumerable: true, configurable: true });
    const EMPTY_F32 = new Float32Array(0);
    const mount = (root, name) => root[name] !== undefined ? root[name] : (root[name] = {});
    const toF32 = (v) => v instanceof Float32Array ? v : Float32Array.from(v);

    // ---- bro.rave ------------------------------------------------------------
    const ns_rave = mount(bro, "rave");
    fn(ns_rave, "init", function init() {
        __bro_native.rave.init();
    });
    fn(ns_rave, "loadRave", function loadRave(modelDir, opts) {
        if (modelDir === undefined) throw new TypeError("bro.rave.loadRave: modelDir is required");
        const d_opts = opts === undefined ? {} : opts;
        return __bro_native.rave.loadRave(modelDir, d_opts.device !== undefined, d_opts.device === undefined ? '' : d_opts.device, d_opts.onReady, d_opts.onError);
    });

    // ---- bro.rave.Rave -------------------------------------------------------
    function Rave() {
        throw new TypeError("bro.rave.Rave is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.rave.RaveProto;
        if (proto === undefined) throw new Error("bro.rave.Rave: native class prototype not published (registerNatives_rave did not run)");
        Object.setPrototypeOf(proto, Rave.prototype);
    }
    fn(mount(bro, "rave"), "Rave", Rave);
    accessor(Rave.prototype, "loaded",
        function () {
            return __bro_native.rave.Rave_loaded_get(this);
        },
        undefined);
    accessor(Rave.prototype, "sampleRate",
        function () {
            return __bro_native.rave.Rave_sampleRate_get(this);
        },
        undefined);
    accessor(Rave.prototype, "nLatent",
        function () {
            return __bro_native.rave.Rave_nLatent_get(this);
        },
        undefined);
    accessor(Rave.prototype, "fullLatent",
        function () {
            return __bro_native.rave.Rave_fullLatent_get(this);
        },
        undefined);
    accessor(Rave.prototype, "nBand",
        function () {
            return __bro_native.rave.Rave_nBand_get(this);
        },
        undefined);
    accessor(Rave.prototype, "totalRatio",
        function () {
            return __bro_native.rave.Rave_totalRatio_get(this);
        },
        undefined);
    fn(Rave.prototype, "encode", function encode(audio) {
        if (audio === undefined) throw new TypeError("bro.rave.Rave.prototype.encode: audio is required");
        __bro_native.rave.Rave_encode(this, toF32(audio));
        return { latent: __bro_native.rave.Rave_encode_latent(), nLatent: __bro_native.rave.Rave_encode_nLatent(), frames: __bro_native.rave.Rave_encode_frames() };
    });
    fn(Rave.prototype, "decode", function decode(latent, frames, opts) {
        if (latent === undefined) throw new TypeError("bro.rave.Rave.prototype.decode: latent is required");
        if (frames === undefined) throw new TypeError("bro.rave.Rave.prototype.decode: frames is required");
        const d_opts = opts === undefined ? {} : opts;
        __bro_native.rave.Rave_decode(this, toF32(latent), frames, d_opts.addNoise === undefined ? false : d_opts.addNoise, d_opts.seed !== undefined, d_opts.seed === undefined ? 0 : d_opts.seed, d_opts.channels === undefined ? 1 : d_opts.channels, d_opts.stereoWidth === undefined ? 1 : d_opts.stereoWidth);
        return { samples: __bro_native.rave.Rave_decode_samples(), sampleRate: __bro_native.rave.Rave_decode_sampleRate(), channels: __bro_native.rave.Rave_decode_channels() };
    });
})();
