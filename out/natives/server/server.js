// server.js — the public shape of bro.server, assembled over the
// natives under __bro_native.server (native_server_decl.h states each C entry point,
// native_server_register.cpp registers them). Generated by brosurface
// (gen/emit_natives.mjs) from idl/server.idl; do not edit.
//
// Every native is spelled by its full dotted path at the point of use: that is the
// spelling the compiler lowers to a direct call. The roots this module reads as bare
// identifiers are listed in module.globals beside it.
(function () {
    'use strict';

    const accessor = (obj, name, get, set) =>
        Object.defineProperty(obj, name, { get, set, enumerable: true, configurable: true });
    const fn = (obj, name, value) =>
        Object.defineProperty(obj, name, { value, writable: true, enumerable: true, configurable: true });
    const mount = (root, name) => root[name] !== undefined ? root[name] : (root[name] = {});

    // ---- bro.server ----------------------------------------------------------
    const ns_server = mount(globalThis.bro, "server");
    accessor(ns_server, "tickrate",
        function () {
            return __bro_native.server.tickrate;
        },
        function (v) {
            __bro_native.server.tickrate = v;
        });
    accessor(ns_server, "uptime",
        function () {
            return __bro_native.server.uptime;
        },
        undefined);
    fn(ns_server, "stop", function stop() {
        __bro_native.server.stop();
    });
})();
