// paths.js — the public shape of bro, assembled over the
// natives under __bro_native.paths (native_paths_decl.h states each C entry point,
// native_paths_register.cpp registers them). Generated by brosurface
// (gen/emit_natives.mjs) from idl/paths.idl; do not edit.
//
// Every native is spelled by its full dotted path at the point of use: that is the
// spelling the compiler lowers to a direct call. The roots this module reads as bare
// identifiers are listed in module.globals beside it.
(function () {
    'use strict';

    const accessor = (obj, name, get, set) =>
        Object.defineProperty(obj, name, { get, set, enumerable: true, configurable: true });
    const fn = (obj, name, value) =>
        Object.defineProperty(obj, name, { value, writable: true, enumerable: true, configurable: true });

    // ---- bro -----------------------------------------------------------------
    const ns_paths = bro;
    accessor(ns_paths, "appDir",
        function () {
            return __bro_native.paths.appDir;
        },
        undefined);
    accessor(ns_paths, "userDataDir",
        function () {
            return __bro_native.paths.userDataDir;
        },
        undefined);
    fn(ns_paths, "resolvePath", function resolvePath(path) {
        if (path === undefined) throw new TypeError("bro.resolvePath: path is required");
        return __bro_native.paths.resolvePath(path);
    });
    fn(ns_paths, "resolveWritePath", function resolveWritePath(path) {
        if (path === undefined) throw new TypeError("bro.resolveWritePath: path is required");
        return __bro_native.paths.resolveWritePath(path);
    });
})();
