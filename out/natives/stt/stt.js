// stt.js — the public shape of bro.stt, bro.stt.WhisperTokenizer, bro.stt.WhisperModel, bro.stt.WhisperSession, bro.stt.ParakeetTokenizer, bro.stt.ParakeetModel, bro.stt.ParakeetSession, bro.stt.QwenAsrModel, bro.stt.QwenAsrSession, bro.stt.QwenAsrStream, assembled over the
// natives under __bro_native.stt (native_stt_decl.h states each C entry point,
// native_stt_register.cpp registers them). Generated by brosurface
// (gen/emit_natives.mjs) from idl/stt.idl; do not edit.
//
// Every native is spelled by its full dotted path at the point of use: that is the
// spelling the compiler lowers to a direct call. The roots this module reads as bare
// identifiers are listed in module.globals beside it.
//
// [manual] members (no native, installed by hand-written JS after this module):
//   bro.stt.WhisperTokenizer.prototype.encode
//   bro.stt.WhisperTokenizer.prototype.decode
//   bro.stt.WhisperTokenizer.prototype.buildPrompt
//   bro.stt.WhisperModel.prototype.transcribe
//   bro.stt.WhisperSession.prototype.transcribe
//   bro.stt.ParakeetTokenizer.prototype.encode
//   bro.stt.ParakeetTokenizer.prototype.decode
//   bro.stt.ParakeetModel.prototype.transcribe
//   bro.stt.ParakeetSession.prototype.transcribe
//   bro.stt.QwenAsrModel.prototype.transcribe
//   bro.stt.QwenAsrSession.prototype.transcribe
//   bro.stt.QwenAsrStream.prototype.feed
//   bro.stt.QwenAsrStream.prototype.finish
//   bro.stt.loadWhisper
//   bro.stt.loadTokenizer
//   bro.stt.loadParakeet
//   bro.stt.loadParakeetTokenizer
//   bro.stt.loadQwenAsr
//   bro.stt.loadQwenAsrStream
//   bro.stt.transcribe
(function () {
    'use strict';

    const accessor = (obj, name, get, set) =>
        Object.defineProperty(obj, name, { get, set, enumerable: true, configurable: true });
    const fn = (obj, name, value) =>
        Object.defineProperty(obj, name, { value, writable: true, enumerable: true, configurable: true });
    const mount = (root, name) => root[name] !== undefined ? root[name] : (root[name] = {});

    // ---- bro.stt -------------------------------------------------------------
    const ns_stt = mount(bro, "stt");
    fn(ns_stt, "init", function init() {
        __bro_native.stt.init();
    });
    // [manual] bro.stt.loadWhisper: operation (dir, opts) — no native is generated; hand-written JS
    // installs it on ns_stt after this module has run.
    // [manual] bro.stt.loadTokenizer: operation (opts) — no native is generated; hand-written JS
    // installs it on ns_stt after this module has run.
    // [manual] bro.stt.loadParakeet: operation (dir, opts) — no native is generated; hand-written JS
    // installs it on ns_stt after this module has run.
    // [manual] bro.stt.loadParakeetTokenizer: operation (path, opts) — no native is generated; hand-written JS
    // installs it on ns_stt after this module has run.
    // [manual] bro.stt.loadQwenAsr: operation (dir, opts) — no native is generated; hand-written JS
    // installs it on ns_stt after this module has run.
    // [manual] bro.stt.loadQwenAsrStream: operation (dir, opts) — no native is generated; hand-written JS
    // installs it on ns_stt after this module has run.
    // [manual] bro.stt.transcribe: operation (model, audio, promptOrOpts, opts) — no native is generated; hand-written JS
    // installs it on ns_stt after this module has run.

    // ---- bro.stt.WhisperTokenizer --------------------------------------------
    function WhisperTokenizer() {
        throw new TypeError("bro.stt.WhisperTokenizer is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.stt.WhisperTokenizerProto;
        if (proto === undefined) throw new Error("bro.stt.WhisperTokenizer: native class prototype not published (registerNatives_stt did not run)");
        Object.setPrototypeOf(proto, WhisperTokenizer.prototype);
    }
    fn(mount(bro, "stt"), "WhisperTokenizer", WhisperTokenizer);
    accessor(WhisperTokenizer.prototype, "loaded",
        function () {
            return __bro_native.stt.WhisperTokenizer_loaded_get(this);
        },
        undefined);
    // [manual] bro.stt.WhisperTokenizer.prototype.encode: operation (text) — no native is generated; hand-written JS
    // installs it on WhisperTokenizer.prototype after this module has run.
    // [manual] bro.stt.WhisperTokenizer.prototype.decode: operation (tokenIds) — no native is generated; hand-written JS
    // installs it on WhisperTokenizer.prototype after this module has run.
    // [manual] bro.stt.WhisperTokenizer.prototype.buildPrompt: operation (opts) — no native is generated; hand-written JS
    // installs it on WhisperTokenizer.prototype after this module has run.

    // ---- bro.stt.WhisperModel ------------------------------------------------
    function WhisperModel() {
        throw new TypeError("bro.stt.WhisperModel is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.stt.WhisperModelProto;
        if (proto === undefined) throw new Error("bro.stt.WhisperModel: native class prototype not published (registerNatives_stt did not run)");
        Object.setPrototypeOf(proto, WhisperModel.prototype);
    }
    fn(mount(bro, "stt"), "WhisperModel", WhisperModel);
    accessor(WhisperModel.prototype, "loaded",
        function () {
            return __bro_native.stt.WhisperModel_loaded_get(this);
        },
        undefined);
    accessor(WhisperModel.prototype, "device",
        function () {
            return __bro_native.stt.WhisperModel_device_get(this);
        },
        undefined);
    // [manual] bro.stt.WhisperModel.prototype.transcribe: operation (audio, promptOrOpts, opts) — no native is generated; hand-written JS
    // installs it on WhisperModel.prototype after this module has run.
    fn(WhisperModel.prototype, "createSession", function createSession() {
        return __bro_native.stt.WhisperModel_createSession(this);
    });

    // ---- bro.stt.WhisperSession ----------------------------------------------
    function WhisperSession() {
        throw new TypeError("bro.stt.WhisperSession is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.stt.WhisperSessionProto;
        if (proto === undefined) throw new Error("bro.stt.WhisperSession: native class prototype not published (registerNatives_stt did not run)");
        Object.setPrototypeOf(proto, WhisperSession.prototype);
    }
    fn(mount(bro, "stt"), "WhisperSession", WhisperSession);
    accessor(WhisperSession.prototype, "loaded",
        function () {
            return __bro_native.stt.WhisperSession_loaded_get(this);
        },
        undefined);
    // [manual] bro.stt.WhisperSession.prototype.transcribe: operation (audio, promptOrOpts, opts) — no native is generated; hand-written JS
    // installs it on WhisperSession.prototype after this module has run.
    fn(WhisperSession.prototype, "reset", function reset() {
        __bro_native.stt.WhisperSession_reset(this);
    });

    // ---- bro.stt.ParakeetTokenizer -------------------------------------------
    function ParakeetTokenizer() {
        throw new TypeError("bro.stt.ParakeetTokenizer is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.stt.ParakeetTokenizerProto;
        if (proto === undefined) throw new Error("bro.stt.ParakeetTokenizer: native class prototype not published (registerNatives_stt did not run)");
        Object.setPrototypeOf(proto, ParakeetTokenizer.prototype);
    }
    fn(mount(bro, "stt"), "ParakeetTokenizer", ParakeetTokenizer);
    accessor(ParakeetTokenizer.prototype, "loaded",
        function () {
            return __bro_native.stt.ParakeetTokenizer_loaded_get(this);
        },
        undefined);
    // [manual] bro.stt.ParakeetTokenizer.prototype.encode: operation (text) — no native is generated; hand-written JS
    // installs it on ParakeetTokenizer.prototype after this module has run.
    // [manual] bro.stt.ParakeetTokenizer.prototype.decode: operation (tokenIds) — no native is generated; hand-written JS
    // installs it on ParakeetTokenizer.prototype after this module has run.

    // ---- bro.stt.ParakeetModel -----------------------------------------------
    function ParakeetModel() {
        throw new TypeError("bro.stt.ParakeetModel is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.stt.ParakeetModelProto;
        if (proto === undefined) throw new Error("bro.stt.ParakeetModel: native class prototype not published (registerNatives_stt did not run)");
        Object.setPrototypeOf(proto, ParakeetModel.prototype);
    }
    fn(mount(bro, "stt"), "ParakeetModel", ParakeetModel);
    accessor(ParakeetModel.prototype, "loaded",
        function () {
            return __bro_native.stt.ParakeetModel_loaded_get(this);
        },
        undefined);
    accessor(ParakeetModel.prototype, "device",
        function () {
            return __bro_native.stt.ParakeetModel_device_get(this);
        },
        undefined);
    // [manual] bro.stt.ParakeetModel.prototype.transcribe: operation (audio, opts) — no native is generated; hand-written JS
    // installs it on ParakeetModel.prototype after this module has run.
    fn(ParakeetModel.prototype, "createSession", function createSession() {
        return __bro_native.stt.ParakeetModel_createSession(this);
    });

    // ---- bro.stt.ParakeetSession ---------------------------------------------
    function ParakeetSession() {
        throw new TypeError("bro.stt.ParakeetSession is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.stt.ParakeetSessionProto;
        if (proto === undefined) throw new Error("bro.stt.ParakeetSession: native class prototype not published (registerNatives_stt did not run)");
        Object.setPrototypeOf(proto, ParakeetSession.prototype);
    }
    fn(mount(bro, "stt"), "ParakeetSession", ParakeetSession);
    accessor(ParakeetSession.prototype, "loaded",
        function () {
            return __bro_native.stt.ParakeetSession_loaded_get(this);
        },
        undefined);
    // [manual] bro.stt.ParakeetSession.prototype.transcribe: operation (audio, opts) — no native is generated; hand-written JS
    // installs it on ParakeetSession.prototype after this module has run.
    fn(ParakeetSession.prototype, "reset", function reset() {
        __bro_native.stt.ParakeetSession_reset(this);
    });

    // ---- bro.stt.QwenAsrModel ------------------------------------------------
    function QwenAsrModel() {
        throw new TypeError("bro.stt.QwenAsrModel is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.stt.QwenAsrModelProto;
        if (proto === undefined) throw new Error("bro.stt.QwenAsrModel: native class prototype not published (registerNatives_stt did not run)");
        Object.setPrototypeOf(proto, QwenAsrModel.prototype);
    }
    fn(mount(bro, "stt"), "QwenAsrModel", QwenAsrModel);
    accessor(QwenAsrModel.prototype, "loaded",
        function () {
            return __bro_native.stt.QwenAsrModel_loaded_get(this);
        },
        undefined);
    accessor(QwenAsrModel.prototype, "device",
        function () {
            return __bro_native.stt.QwenAsrModel_device_get(this);
        },
        undefined);
    // [manual] bro.stt.QwenAsrModel.prototype.transcribe: operation (audio, opts) — no native is generated; hand-written JS
    // installs it on QwenAsrModel.prototype after this module has run.
    fn(QwenAsrModel.prototype, "createSession", function createSession() {
        return __bro_native.stt.QwenAsrModel_createSession(this);
    });

    // ---- bro.stt.QwenAsrSession ----------------------------------------------
    function QwenAsrSession() {
        throw new TypeError("bro.stt.QwenAsrSession is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.stt.QwenAsrSessionProto;
        if (proto === undefined) throw new Error("bro.stt.QwenAsrSession: native class prototype not published (registerNatives_stt did not run)");
        Object.setPrototypeOf(proto, QwenAsrSession.prototype);
    }
    fn(mount(bro, "stt"), "QwenAsrSession", QwenAsrSession);
    accessor(QwenAsrSession.prototype, "loaded",
        function () {
            return __bro_native.stt.QwenAsrSession_loaded_get(this);
        },
        undefined);
    // [manual] bro.stt.QwenAsrSession.prototype.transcribe: operation (audio, opts) — no native is generated; hand-written JS
    // installs it on QwenAsrSession.prototype after this module has run.
    fn(QwenAsrSession.prototype, "reset", function reset() {
        __bro_native.stt.QwenAsrSession_reset(this);
    });

    // ---- bro.stt.QwenAsrStream -----------------------------------------------
    function QwenAsrStream() {
        throw new TypeError("bro.stt.QwenAsrStream is not constructible: instances come from the natives that return one");
    }
    {
        const proto = __bro_native.stt.QwenAsrStreamProto;
        if (proto === undefined) throw new Error("bro.stt.QwenAsrStream: native class prototype not published (registerNatives_stt did not run)");
        Object.setPrototypeOf(proto, QwenAsrStream.prototype);
    }
    fn(mount(bro, "stt"), "QwenAsrStream", QwenAsrStream);
    accessor(QwenAsrStream.prototype, "loaded",
        function () {
            return __bro_native.stt.QwenAsrStream_loaded_get(this);
        },
        undefined);
    // [manual] bro.stt.QwenAsrStream.prototype.feed: operation (audio) — no native is generated; hand-written JS
    // installs it on QwenAsrStream.prototype after this module has run.
    // [manual] bro.stt.QwenAsrStream.prototype.finish: operation () — no native is generated; hand-written JS
    // installs it on QwenAsrStream.prototype after this module has run.
})();
