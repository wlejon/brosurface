// native_stt_decl.h — the C entry points behind idl/stt.idl, one prototype per
// native. Generated by brosurface (gen/emit_natives.mjs); do not edit. The bodies
// are hand-written in bro against the engine, and the compiler checks them here.
//
// How each type crosses (schema/native_types.mjs, bronze/src/abi/bronze_native_type.h):
//   double / int32_t / bool      f64 / i32 / bool
//   const char*                  str — UTF-8, valid for the call only; a RESULT must
//                                outlive the return (a per-thread scratch), and one
//                                marked JSON carries JSON of the IDL type in the comment
//   uint64_t                     dynamic — a callback's raw Value bits; keep it in a
//                                Persistent, never across an allocating call raw
//   (const T*, uint32_t)         a typed array: elements and count, valid for the call
//   void*                        a class handle: what the class constructor returned
//   bronze_native_buffer* out    a typed-array RESULT: the function returns void and
//                                fills *out (data, length in elements); release == NULL
//                                and the runtime copies during the call; release set
//                                ([transfer]) and the array is a view over data, with
//                                release(ctx) run when it is collected
//   bool <x>_given, T <x>        an optional value with no declared default, and
//                                whether it was passed
//
// A dictionary or list RESULT crosses as reads: the operation runs and keeps its
// result in a per-thread slot, answering nothing (or the list's length), and the
// <op>_<member> / <op>_at natives read from that slot until the next call.
#pragma once

#include <stdbool.h>
#include <stdint.h>

#include "abi/bronze_native_type.h"  // bronze_native_buffer, owned by bronze

#ifdef __cplusplus
extern "C" {
#endif

// destructor of __bro_native.stt.WhisperTokenizer: frees what the constructor (or a native returning a WhisperTokenizer) allocated
void bro_stt_WhisperTokenizer_dtor(void* self);

// constructor of __bro_native.stt.WhisperTokenizer: registered so the class exists (instances come from natives returning a WhisperTokenizer); never called by the wrapper, the body may return NULL
//   registered at __bro_native.stt.WhisperTokenizer
void* bro_stt_WhisperTokenizer_ctor(void);

// destructor of __bro_native.stt.WhisperModel: frees what the constructor (or a native returning a WhisperModel) allocated
void bro_stt_WhisperModel_dtor(void* self);

// constructor of __bro_native.stt.WhisperModel: registered so the class exists (instances come from natives returning a WhisperModel); never called by the wrapper, the body may return NULL
//   registered at __bro_native.stt.WhisperModel
void* bro_stt_WhisperModel_ctor(void);

// destructor of __bro_native.stt.WhisperSession: frees what the constructor (or a native returning a WhisperSession) allocated
void bro_stt_WhisperSession_dtor(void* self);

// constructor of __bro_native.stt.WhisperSession: registered so the class exists (instances come from natives returning a WhisperSession); never called by the wrapper, the body may return NULL
//   registered at __bro_native.stt.WhisperSession
void* bro_stt_WhisperSession_ctor(void);

// destructor of __bro_native.stt.ParakeetTokenizer: frees what the constructor (or a native returning a ParakeetTokenizer) allocated
void bro_stt_ParakeetTokenizer_dtor(void* self);

// constructor of __bro_native.stt.ParakeetTokenizer: registered so the class exists (instances come from natives returning a ParakeetTokenizer); never called by the wrapper, the body may return NULL
//   registered at __bro_native.stt.ParakeetTokenizer
void* bro_stt_ParakeetTokenizer_ctor(void);

// destructor of __bro_native.stt.ParakeetModel: frees what the constructor (or a native returning a ParakeetModel) allocated
void bro_stt_ParakeetModel_dtor(void* self);

// constructor of __bro_native.stt.ParakeetModel: registered so the class exists (instances come from natives returning a ParakeetModel); never called by the wrapper, the body may return NULL
//   registered at __bro_native.stt.ParakeetModel
void* bro_stt_ParakeetModel_ctor(void);

// destructor of __bro_native.stt.ParakeetSession: frees what the constructor (or a native returning a ParakeetSession) allocated
void bro_stt_ParakeetSession_dtor(void* self);

// constructor of __bro_native.stt.ParakeetSession: registered so the class exists (instances come from natives returning a ParakeetSession); never called by the wrapper, the body may return NULL
//   registered at __bro_native.stt.ParakeetSession
void* bro_stt_ParakeetSession_ctor(void);

// destructor of __bro_native.stt.QwenAsrModel: frees what the constructor (or a native returning a QwenAsrModel) allocated
void bro_stt_QwenAsrModel_dtor(void* self);

// constructor of __bro_native.stt.QwenAsrModel: registered so the class exists (instances come from natives returning a QwenAsrModel); never called by the wrapper, the body may return NULL
//   registered at __bro_native.stt.QwenAsrModel
void* bro_stt_QwenAsrModel_ctor(void);

// destructor of __bro_native.stt.QwenAsrSession: frees what the constructor (or a native returning a QwenAsrSession) allocated
void bro_stt_QwenAsrSession_dtor(void* self);

// constructor of __bro_native.stt.QwenAsrSession: registered so the class exists (instances come from natives returning a QwenAsrSession); never called by the wrapper, the body may return NULL
//   registered at __bro_native.stt.QwenAsrSession
void* bro_stt_QwenAsrSession_ctor(void);

// destructor of __bro_native.stt.QwenAsrStream: frees what the constructor (or a native returning a QwenAsrStream) allocated
void bro_stt_QwenAsrStream_dtor(void* self);

// constructor of __bro_native.stt.QwenAsrStream: registered so the class exists (instances come from natives returning a QwenAsrStream); never called by the wrapper, the body may return NULL
//   registered at __bro_native.stt.QwenAsrStream
void* bro_stt_QwenAsrStream_ctor(void);

// bro.stt.WhisperTokenizer.prototype.loaded read
//   registered at __bro_native.stt.WhisperTokenizer_loaded_get
bool bro_stt_WhisperTokenizer_loaded_get(void* self);

// bro.stt.WhisperModel.prototype.loaded read
//   registered at __bro_native.stt.WhisperModel_loaded_get
bool bro_stt_WhisperModel_loaded_get(void* self);

// bro.stt.WhisperModel.prototype.device read
//   registered at __bro_native.stt.WhisperModel_device_get
const char* bro_stt_WhisperModel_device_get(void* self);

// bro.stt.WhisperModel.prototype.createSession
//   registered at __bro_native.stt.WhisperModel_createSession
void* bro_stt_WhisperModel_createSession(void* self);

// bro.stt.WhisperSession.prototype.loaded read
//   registered at __bro_native.stt.WhisperSession_loaded_get
bool bro_stt_WhisperSession_loaded_get(void* self);

// bro.stt.WhisperSession.prototype.reset
//   registered at __bro_native.stt.WhisperSession_reset
void bro_stt_WhisperSession_reset(void* self);

// bro.stt.ParakeetTokenizer.prototype.loaded read
//   registered at __bro_native.stt.ParakeetTokenizer_loaded_get
bool bro_stt_ParakeetTokenizer_loaded_get(void* self);

// bro.stt.ParakeetModel.prototype.loaded read
//   registered at __bro_native.stt.ParakeetModel_loaded_get
bool bro_stt_ParakeetModel_loaded_get(void* self);

// bro.stt.ParakeetModel.prototype.device read
//   registered at __bro_native.stt.ParakeetModel_device_get
const char* bro_stt_ParakeetModel_device_get(void* self);

// bro.stt.ParakeetModel.prototype.createSession
//   registered at __bro_native.stt.ParakeetModel_createSession
void* bro_stt_ParakeetModel_createSession(void* self);

// bro.stt.ParakeetSession.prototype.loaded read
//   registered at __bro_native.stt.ParakeetSession_loaded_get
bool bro_stt_ParakeetSession_loaded_get(void* self);

// bro.stt.ParakeetSession.prototype.reset
//   registered at __bro_native.stt.ParakeetSession_reset
void bro_stt_ParakeetSession_reset(void* self);

// bro.stt.QwenAsrModel.prototype.loaded read
//   registered at __bro_native.stt.QwenAsrModel_loaded_get
bool bro_stt_QwenAsrModel_loaded_get(void* self);

// bro.stt.QwenAsrModel.prototype.device read
//   registered at __bro_native.stt.QwenAsrModel_device_get
const char* bro_stt_QwenAsrModel_device_get(void* self);

// bro.stt.QwenAsrModel.prototype.createSession
//   registered at __bro_native.stt.QwenAsrModel_createSession
void* bro_stt_QwenAsrModel_createSession(void* self);

// bro.stt.QwenAsrSession.prototype.loaded read
//   registered at __bro_native.stt.QwenAsrSession_loaded_get
bool bro_stt_QwenAsrSession_loaded_get(void* self);

// bro.stt.QwenAsrSession.prototype.reset
//   registered at __bro_native.stt.QwenAsrSession_reset
void bro_stt_QwenAsrSession_reset(void* self);

// bro.stt.QwenAsrStream.prototype.loaded read
//   registered at __bro_native.stt.QwenAsrStream_loaded_get
bool bro_stt_QwenAsrStream_loaded_get(void* self);

#if BRO_WITH_SOUNDML
// bro.stt.init
//   registered at __bro_native.stt.init
void bro_stt_init(void);
#endif

#ifdef __cplusplus
}  // extern "C"
#endif
