// native_motion_decl.h — the C entry points behind idl/motion.idl, one prototype per
// native. Generated by brosurface (gen/emit_natives.mjs); do not edit. The bodies
// are hand-written in bro against the engine, and the compiler checks them here.
//
// How each type crosses (schema/native_types.mjs, bronze/src/abi/bronze_native_type.h):
//   double / int32_t / bool      f64 / i32 / bool
//   const char*                  str — UTF-8, valid for the call only; a RESULT must
//                                outlive the return (a per-thread scratch), and one
//                                marked JSON carries JSON of the IDL type in the comment
//   uint64_t                     dynamic — a callback's raw Value bits; keep it in a
//                                Persistent, never across an allocating call raw
//   (const T*, uint32_t)         a typed array: elements and count, valid for the call
//   void*                        a class handle: what the class constructor returned
//   bronze_native_buffer* out    a typed-array RESULT: the function returns void and
//                                fills *out (data, length in elements); release == NULL
//                                and the runtime copies during the call; release set
//                                ([transfer]) and the array is a view over data, with
//                                release(ctx) run when it is collected
//   bool <x>_given, T <x>        an optional value with no declared default, and
//                                whether it was passed
//
// A dictionary or list RESULT crosses as reads: the operation runs and keeps its
// result in a per-thread slot, answering nothing (or the list's length), and the
// <op>_<member> / <op>_at natives read from that slot until the next call.
#pragma once

#include <stdbool.h>
#include <stdint.h>

#include "abi/bronze_native_type.h"  // bronze_native_buffer, owned by bronze

#ifdef __cplusplus
extern "C" {
#endif

// destructor of __bro_native.motion.ArdyMotionPipeline: frees what the constructor (or a native returning a ArdyMotionPipeline) allocated
void bro_motion_ArdyMotionPipeline_dtor(void* self);

// constructor of __bro_native.motion.ArdyMotionPipeline: registered so the class exists (instances come from natives returning a ArdyMotionPipeline); never called by the wrapper, the body may return NULL
//   registered at __bro_native.motion.ArdyMotionPipeline
void* bro_motion_ArdyMotionPipeline_ctor(void);

// bro.motion.ArdyMotionPipeline.prototype.device read
//   registered at __bro_native.motion.ArdyMotionPipeline_device_get
const char* bro_motion_ArdyMotionPipeline_device_get(void* self);

// bro.motion.ArdyMotionPipeline.prototype.generate: runs the operation and keeps its MotionClip result in a per-thread slot the ArdyMotionPipeline_generate_<member> reads answer from until the next call
//   registered at __bro_native.motion.ArdyMotionPipeline_generate
void bro_motion_ArdyMotionPipeline_generate(void* self, const char* text, int32_t opts_frames, int32_t opts_steps, double opts_cfg, int32_t opts_seed, double opts_heading);

// MotionClip.frames of the result ArdyMotionPipeline_generate kept
//   registered at __bro_native.motion.ArdyMotionPipeline_generate_frames
int32_t bro_motion_ArdyMotionPipeline_generate_frames(void);

// MotionClip.joints of the result ArdyMotionPipeline_generate kept
//   registered at __bro_native.motion.ArdyMotionPipeline_generate_joints
int32_t bro_motion_ArdyMotionPipeline_generate_joints(void);

// MotionClip.fps of the result ArdyMotionPipeline_generate kept
//   registered at __bro_native.motion.ArdyMotionPipeline_generate_fps
double bro_motion_ArdyMotionPipeline_generate_fps(void);

// MotionClip.positions of the result ArdyMotionPipeline_generate kept
//   registered at __bro_native.motion.ArdyMotionPipeline_generate_positions
//   result: f32[] in *out; out->release == NULL, the runtime copies
void bro_motion_ArdyMotionPipeline_generate_positions(bronze_native_buffer* out);

// MotionClip.parents of the result ArdyMotionPipeline_generate kept
//   registered at __bro_native.motion.ArdyMotionPipeline_generate_parents
//   result: i32[] in *out; out->release == NULL, the runtime copies
void bro_motion_ArdyMotionPipeline_generate_parents(bronze_native_buffer* out);

// MotionClip.footContacts of the result ArdyMotionPipeline_generate kept
//   registered at __bro_native.motion.ArdyMotionPipeline_generate_footContacts
//   result: f32[] in *out; out->release == NULL, the runtime copies
void bro_motion_ArdyMotionPipeline_generate_footContacts(bronze_native_buffer* out);

#if BRO_WITH_DIFFUSION && BRO_WITH_LM
// bro.motion.init
//   registered at __bro_native.motion.init
void bro_motion_init(void);
#endif

#if BRO_WITH_DIFFUSION && BRO_WITH_LM
// bro.motion.load
//   registered at __bro_native.motion.load
//   opts_checkpoint_given, opts_textEncoder_given, opts_device_given: false when the value was not passed (no declared default)
void* bro_motion_load(bool opts_checkpoint_given, const char* opts_checkpoint, bool opts_textEncoder_given, const char* opts_textEncoder, bool opts_device_given, const char* opts_device);
#endif

#ifdef __cplusplus
}  // extern "C"
#endif
