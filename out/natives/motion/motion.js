// motion.js — the public shape of bro.motion, bro.motion.ArdyMotionPipeline, assembled over the
// natives under __bro_native.motion (native_motion_decl.h states each C entry point,
// native_motion_register.cpp registers them). Generated by brosurface
// (gen/emit_natives.mjs) from idl/motion.idl; do not edit.
//
// Every native is spelled by its full dotted path at the point of use: that is the
// spelling the compiler lowers to a direct call. The roots this module reads as bare
// identifiers are listed in module.globals beside it.
(function () {
    'use strict';

    const accessor = (obj, name, get, set) =>
        Object.defineProperty(obj, name, { get, set, enumerable: true, configurable: true });
    const fn = (obj, name, value) =>
        Object.defineProperty(obj, name, { value, writable: true, enumerable: true, configurable: true });
    const mount = (root, name) => root[name] !== undefined ? root[name] : (root[name] = {});

    // ---- bro.motion ----------------------------------------------------------
    const ns_motion = mount(globalThis.bro, "motion");
    fn(ns_motion, "init", function init() {
        __bro_native.motion.init();
    });
    fn(ns_motion, "load", function load(opts) {
        if (opts === undefined) throw new TypeError("bro.motion.load: opts is required");
        const d_opts = opts;
        if (d_opts.checkpoint !== undefined && typeof d_opts.checkpoint !== 'string') throw new TypeError("bro.motion.load: opts.checkpoint must be a string");
        if (d_opts.textEncoder !== undefined && typeof d_opts.textEncoder !== 'string') throw new TypeError("bro.motion.load: opts.textEncoder must be a string");
        if (d_opts.device !== undefined && typeof d_opts.device !== 'string') throw new TypeError("bro.motion.load: opts.device must be a string");
        return __bro_native.motion.load(d_opts.checkpoint !== undefined, d_opts.checkpoint === undefined ? '' : d_opts.checkpoint, d_opts.textEncoder !== undefined, d_opts.textEncoder === undefined ? '' : d_opts.textEncoder, d_opts.device !== undefined, d_opts.device === undefined ? '' : d_opts.device);
    });

    // ---- bro.motion.ArdyMotionPipeline ---------------------------------------
    function ArdyMotionPipeline() {
        throw new TypeError("bro.motion.ArdyMotionPipeline is not constructible: instances come from the natives that return one");
    }
    {
        const proto = globalThis.__bro_native.motion.ArdyMotionPipelineProto;
        if (proto === undefined) throw new Error("bro.motion.ArdyMotionPipeline: native class prototype not published (registerNatives_motion did not run)");
        Object.setPrototypeOf(proto, ArdyMotionPipeline.prototype);
    }
    fn(mount(globalThis.bro, "motion"), "ArdyMotionPipeline", ArdyMotionPipeline);
    accessor(ArdyMotionPipeline.prototype, "device",
        function () {
            return __bro_native.motion.ArdyMotionPipeline_device_get(this);
        },
        undefined);
    fn(ArdyMotionPipeline.prototype, "generate", function generate(text, opts) {
        if (text === undefined) throw new TypeError("bro.motion.ArdyMotionPipeline.prototype.generate: text is required");
        const d_opts = opts === undefined ? {} : opts;
        __bro_native.motion.ArdyMotionPipeline_generate(this, text, d_opts.frames === undefined ? 104 : d_opts.frames, d_opts.steps === undefined ? 10 : d_opts.steps, d_opts.cfg === undefined ? 2.5 : d_opts.cfg, d_opts.seed === undefined ? 0 : d_opts.seed, d_opts.heading === undefined ? 0 : d_opts.heading);
        return { frames: __bro_native.motion.ArdyMotionPipeline_generate_frames(), joints: __bro_native.motion.ArdyMotionPipeline_generate_joints(), fps: __bro_native.motion.ArdyMotionPipeline_generate_fps(), positions: __bro_native.motion.ArdyMotionPipeline_generate_positions(), parents: __bro_native.motion.ArdyMotionPipeline_generate_parents(), footContacts: __bro_native.motion.ArdyMotionPipeline_generate_footContacts() };
    });
})();
