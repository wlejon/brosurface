// native_net_decl.h — the C entry points behind idl/net.idl, one prototype per
// native. Generated by brosurface (gen/emit_natives.mjs); do not edit. The bodies
// are hand-written in bro against the engine, and the compiler checks them here.
//
// How each type crosses (schema/native_types.mjs, bronze/src/abi/bronze_native_type.h):
//   double / int32_t / bool      f64 / i32 / bool
//   const char*                  str — UTF-8, valid for the call only; a RESULT must
//                                outlive the return (a per-thread scratch), and one
//                                marked JSON carries JSON of the IDL type in the comment
//   uint64_t                     dynamic — a callback's raw Value bits; keep it in a
//                                Persistent, never across an allocating call raw
//   (const T*, uint32_t)         a typed array: elements and count, valid for the call
//   void*                        a class handle: what the class constructor returned
//   bronze_native_buffer* out    a typed-array RESULT: the function returns void and
//                                fills *out (data, length in elements); release == NULL
//                                and the runtime copies during the call; release set
//                                ([transfer]) and the array is a view over data, with
//                                release(ctx) run when it is collected
//   bool <x>_given, T <x>        an optional value with no declared default, and
//                                whether it was passed
//
// A dictionary or list RESULT crosses as reads: the operation runs and keeps its
// result in a per-thread slot, answering nothing (or the list's length), and the
// <op>_<member> / <op>_at natives read from that slot until the next call.
//
// Gate: BRO_WITH_NET (native_net_register.cpp registers nothing when it is off).
#pragma once

#include <stdbool.h>
#include <stdint.h>

#include "abi/bronze_native_type.h"  // bronze_native_buffer, owned by bronze

#ifdef __cplusplus
extern "C" {
#endif

// bro.net.host
//   registered at __bro_native.net.host
void bro_net_host(int32_t port, uint64_t callback);

// bro.net.unhost
//   registered at __bro_native.net.unhost
void bro_net_unhost(void);

// bro.net.connect
//   registered at __bro_native.net.connect
int32_t bro_net_connect(const char* address, int32_t port, uint64_t callback);

// bro.net.disconnect
//   registered at __bro_native.net.disconnect
void bro_net_disconnect(int32_t peerId);

// bro.net.disconnectAll
//   registered at __bro_native.net.disconnectAll
void bro_net_disconnectAll(void);

// bro.net.send
//   registered at __bro_native.net.send
void bro_net_send(int32_t peerId, const uint8_t* data, uint32_t data_len, int32_t channel);

// bro.net.broadcast
//   registered at __bro_native.net.broadcast
void bro_net_broadcast(const uint8_t* data, uint32_t data_len, int32_t channel);

// bro.net.peers
//   registered at __bro_native.net.peers
//   result: i32[] in *out; out->release == NULL, the runtime copies
void bro_net_peers(bronze_native_buffer* out);

// bro.net.getPeerAddress
//   registered at __bro_native.net.getPeerAddress
const char* bro_net_getPeerAddress(int32_t peerId);

// bro.net.stats
//   registered at __bro_native.net.stats
//   result: JSON of NetStats
const char* bro_net_stats(void);

// bro.net.getPeerStats
//   registered at __bro_native.net.getPeerStats
//   result: JSON of NetStats
const char* bro_net_getPeerStats(int32_t peerId);

// bro.net.setPeerSimulatedLoss
//   registered at __bro_native.net.setPeerSimulatedLoss
void bro_net_setPeerSimulatedLoss(int32_t peerId, double chance, double latencyMin, double latencyMax);

#ifdef __cplusplus
}  // extern "C"
#endif
